# skills/ - 工作区专用 Skills

每项能力使用独立目录：

```text
skills/<skill-name>/SKILL.md
```

Skill 描述“如何完成任务”，并不授予文件、网络或命令权限。对应工具必须先在 `openclaw.json` 中显式开放，并完成安全与端到端测试。

当前已创建但尚未授权上线：

- `personal-weather/SKILL.md`：天气查询及未来最近出行记录的行为边界。

计划继续按知识库维护、文件处理、coder Agent/Git 工作流的顺序逐项开发。
