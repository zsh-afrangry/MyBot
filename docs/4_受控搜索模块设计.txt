kurumi 受控搜索与模型能力架构设计

首次设计：2026-08-12
最后更新：2026-08-18（DeepSeek、OpenClaw web_search/Terra Responses、受控回滚、QQ Eval 与 MVP 收尾门槛拆分）
状态：架构设计与验证阶段；DeepSeek 已接入受控 Adapter，并通过主人 QQ 私聊最终正向验收；
OpenClaw 通用 `web_search` 和自定义 Terra `/responses` 传输已完成诊断验证，最终常驻权限已收敛为
`personal_web_search`；Terra 原生 `web_search` 尚未通过 OpenClaw 自定义 Provider 接入，
`web_fetch` 和 browser 仍未开放，完整 S3 验收尚未完成。

本文中的相对路径均以 OpenClaw 项目根目录为基准。本文描述设计与开发计划，不授予运行时
权限；实际能力以 `openclaw.json`、运行时工具清单和端到端验收为准。任何 API Key、Token、
账号或完整 QQ 身份标识都决定是否接写入本文。

TODO（随开发更新）：

- [x] A0：明确模型认知过程、基础能力、领域能力、内部控制能力与横切切面的边界。
- [x] A1：确认认知动作默认属于 LLM Runtime，不批量注册为 Capability/Tool。
- [x] A2：确认 Plan IR 第一版只实现 `single`、`sequence`、`scheduled`，不开发通用 DAG 引擎。
- [ ] S0：冻结首版范围、威胁模型、Provider/费用/隐私决策和接口契约。
- [ ] S1：在不改运行时权限的前提下，实现独立 `personal-search` 插件的离线最小切片。
- [ ] S2：完成离线功能、安全、故障、配额、隐私和既有模块回归验收。
- [ ] S3：分阶段接入 OpenClaw，并完成主人 QQ 私聊及权限负向的真实验收和回滚演练。
- [ ] S4：在 S3 稳定后完善确定性搜索路由、引用规范和质量 Eval；不开发动态 Router。
- [ ] S5：只有出现真实组合/延迟搜索用例后，才实现固定 `sequence` / `scheduled` 模板。
- [ ] S6：只有结果列表稳定后，才评估“打开本次搜索返回的单条 result ID”；不得接受任意 URL。
- [ ] A3：只有真实任务需要并行依赖、汇合或补偿时，才评审 DAG Workflow Engine。

DeepSeek 优先验证轨道（D 项不能替代 S0-S3 的上线门槛）：

- [x] D0.1：完成 DeepSeek 静态配置、SecretRef、Gateway 环境和无泄密检查。
- [x] D0.2：完成 DeepSeek Responses API 的真实 Web Search smoke test；固定公开 query，验证
  `/responses`、`deepseek-v4-flash`、`web_search`、`web_search_call`、完成状态和来源 URL。
- [x] D0.3：完成认证失败、超时、429/5xx、异常响应和超大响应的离线错误契约测试；确认不自动
  重复请求。真实 Provider 故障、权限和费用侧验收仍归入 D3.1/S3.7。
- [x] D1.1：用脱敏真实响应建立 parser/normalizer fixture，转换为受控 `kind: "answer"`、最多
  5 条 citations 和 `Provenance`，保留来源、时间和不可信内容标记。
- [x] D1.2：完成“GPT-Terra 主 Agent -> `personal_web_search` -> DeepSeek”委派链路测试（离线
  tool/runtime 替身）；验证
  主 Agent 只传递受约束 query，最终回答使用来源证据，不把 DeepSeek 摘要当作无来源事实。
- [x] D1.3：完成视觉边界离线契约测试；含图片、图表、扫描 PDF 的问题明确标记为未完成视觉核验，
  不把文本检索 Worker 当作视觉路径。真实带附件的主 Agent 端到端行为仍需按模型能力另行验收。
- [x] D2.1：完成 Provider/Adapter 与 OpenClaw runtime 的离线接入测试；受控包装工具仍需精确权限
  变更后才可见，`web_fetch`、browser 或 exec 未开放。通用 `web_search` 的后续诊断性开放另见
  D2.2，不作为受控 Search MVP 已上线的依据。
- [x] D2.2：完成 OpenClaw 通用 `web_search` 的主人 QQ 私聊正向验收；确认工具实际调用
  `web_search`，并由已选的 `deepseek-search` Provider 返回 `kind: "answer"`。
- [x] D2.3：完成自定义 Terra `openai-responses` Provider 的 QQ 验收；确认 OpenClaw 实际请求
  `POST /v1/responses`，并确认通用 `web_search` 可以作为 OpenClaw 工具运行在该 Responses 回合中。
- [ ] D2.4：若仍需要 Terra 原生 `tools: [{"type":"web_search"}]`，单独设计并验收适配层；当前
  OpenClaw 内置原生搜索注入只匹配官方 `openai` Provider 和官方 OpenAI API Base URL，不会对
  自定义 `naiccc` Provider 自动注入。
- [ ] D3.1：完成主人 QQ 私聊正向验收，以及群聊/非主人/无 Key/上游失败的负向验收；目前主人
  私聊正向、当前事件与小众 query 成功样例、撤权、插件禁用/恢复和离线无 Key/上游故障已覆盖，
  群聊/非主人仍待收尾，主模型真实上游失败的可观测错误路径已记录但降级策略尚未固化；结果通过
  后才能将 S0.2、S3.3 等对应门槛更新为完成。

各阶段的可执行子任务、验收目标、停止条件和回滚边界见第十三节。顶部 S0-S3 阶段项以该阶段
全部 Must Pass 完成为准；Should Pass 可以作为稳定化债务继续保留。“代码已写”不等于“能力
已验收”，但非阻塞治理项也不得阻止一个边界已经足够安全的 MVP 首次运行。

======================================================================
一、本轮结论
======================================================================

1. “模型能力”不是一个单独字段，也不全部属于执行策略。
2. 模型的理解、抽取、规划、比较、综合和生成默认是 Agent/LLM Runtime 的内部认知过程，
   不为每个动词注册 `model.extract`、`model.plan`、`model.synthesize` 等 Tool。
3. 只有某个认知阶段具有独立模型、计费、超时、缓存、验证或重试需求时，才把它提升为显式
   Plan Step。此时它是可独立执行的“模型阶段”，不是把所有思考动作工具化。
4. Calculator、Current Time、Web Search、OCR 等是可复用的 Primitive Capability。它们会改变
   任务输入或结果，必须作为显式依赖/调用留下来源，不能像日志一样透明插入。
5. Weather、Stock、Paper、Git、Travel 等是 Domain Capability；领域操作可以声明并复用基础
   能力，但不会自动继承基础能力的权限。
6. Scheduler、Workflow、Notification 是内部 Control Plane。领域服务可以通过受限 adapter 使用，
   但不因此把通用 Scheduler、任意发送或 Workflow 管理权限交给模型。
7. 身份检查、权限、审计、限流、预算、追踪、输入输出校验、不可信内容标记和幂等适合采用
   AOP/中间件式横切设计。
8. 数据模型可为 DAG 预留版本和依赖字段；第一版执行器不实现任意 DAG、分支、循环或模型生成
   的工作流。

一句话概括：

  模型负责认知；显式 Capability 提供可验证的外部能力；领域操作声明依赖；控制平面负责触发和
  投递；横切中间件在每次调用前后强制执行权限与安全策略。

======================================================================
二、避免“模型能力”概念膨胀
======================================================================

认知能力在架构中是一等概念，但默认不是 Capability Registry 中的一等 Tool。

建议的概念模型：

  LLM Runtime
  ├── understanding
  ├── extraction
  ├── planning
  ├── comparison
  ├── synthesis
  └── generation

普通任务不需要把这些过程序列化。例如：

  用户问天气
      -> Agent 理解问题
      -> weather.get_brief
      -> Agent 组织回答

不创建：

  model.extract -> weather.get_brief -> model.synthesize

否则 Registry 很快会膨胀为 `model.classify/reason/compare/rewrite/translate/rank/reflect/...`，
只是把模型内部动词重新包装成几十个“工具”，既增加上下文，也不能带来稳定的执行边界。

显式提升模型阶段的条件：满足以下任一真实需求时才考虑。

- 使用不同模型或不同 Provider；
- 独立计费、预算或 Token 上限；
- 独立超时、重试或 fallback；
- 独立缓存且输入输出可以稳定寻址；
- 需要结构化输出校验或专门 Eval；
- 需要第二模型复核、人工确认或可审计重放；
- 该阶段的失败需要与其他步骤分别恢复。

例如复杂金融分析可以是：

  stock.get_kline
      -> web.search
      -> model.financial_analysis（专用模型、结构化输出）
      -> model.review（第二模型、独立验证）

此处两个模型阶段有独立执行语义，才值得成为显式 Step。

需要区分：

- “模型能够理解/总结”属于 LLM Runtime 的能力事实；
- “这一任务要不要独立运行一个模型阶段”属于 Plan；
- “选择哪个模型、thinking level、预算、超时和 fallback”属于 ExecutionPolicy。

======================================================================
三、任务表达式与 Plan IR
======================================================================

此前的设计为：

  任务 = 能力 × 操作 × 目标 × 触发方式 × 执行策略 × 权限策略 × 投递方式

它继续作为概念检查表使用，不直接等同于 v1 的序列化 schema。

保留这套分解，但显式执行计划是可选项，且第一版不强制设置全局 Target：

  TaskSpec
  ├── Goal
  ├── ExplicitPlan?          // 可选；仅在需要显式 Capability 时存在
  ├── Trigger
  ├── ExecutionPolicy
  ├── PermissionPolicy
  └── DeliveryPolicy

其中：

- Goal：用户真正想得到的结果；
- ExplicitPlan：可选的显式能力调用，不包含普通的隐式理解和措辞过程；
- Trigger：立即、绝对时间或受控周期；
- ExecutionPolicy：模型/Provider、超时、重试、预算、缓存、fallback、失败策略；
- PermissionPolicy：谁、在哪个通道、对什么数据和副作用有权限；
- DeliveryPolicy：结果送到哪里、格式、去重、静默时段和失败可见性。

纯模型任务可以没有 ExplicitPlan。例如改写句子、解释稳定知识或总结当前上下文时，LLM Runtime
直接围绕 Goal 工作，不为了统一格式虚构 `model.generate`、`model.explain` 等 Capability。

第一版也不设置容易产生歧义的全局 Target。目标放在真正使用它的局部位置：

- `Step.target`：该能力步骤操作的对象，例如 `NVDA` 或“最近 30 天 NVIDIA 新闻”；
- `DeliveryPolicy.target`：投递对象，例如当前主人 QQ 私聊；
- `Goal`：保留跨步骤的全局意图。

若未来多个领域证明存在稳定且唯一的“任务主体”，再通过新 schemaVersion 增加可选 TaskTarget，
不在 v1 提前占位。

第一版 Plan IR 只支持三种外形：

1. `single`

   一个显式能力调用，例如 `weather.get_brief` 或 `web.search`。

2. `sequence`

   少量固定顺序步骤；前一步的受控输出成为后一步输入。首版不支持任意分支、循环、动态新增
   Step 或模型生成命令。

3. `scheduled`

   对 `single` 或 `sequence` 增加受控 Trigger。语义上 Schedule 属于 Trigger，不是新的计算
   拓扑；序列化格式可以提供 `scheduled` 包装，便于实现和阅读。

示意结构：

  {
    "schemaVersion": 1,
    "goal": "查询公开网页中的最新信息",
    "explicitPlan": {
      "kind": "single",
      "step": {
        "capability": "web.search",
        "operation": "search",
        "target": "public_web"
      }
    },
    "trigger": { "kind": "now" },
    "executionPolicy": {
      "timeoutMs": 8000,
      "maxAttempts": 1,
      "fallback": "none"
    },
    "permissionPolicy": {
      "context": "owner_qq_private",
      "dataEgress": "fixed_search_provider"
    },
    "deliveryPolicy": {
      "kind": "reply_private_chat",
      "target": "trusted_owner_from_context"
    }
  }

不要求每一次普通对话都持久化 TaskSpec，也不要求每个 TaskSpec 都含 ExplicitPlan。`single` 可只在
本轮内存中执行；只有定时、可恢复、多步写入或需要审计重放的任务才需要持久化结构化计划。

未来可在 schema 中预留 `stepId`、`dependsOn`，但 v1 执行器忽略/拒绝并行依赖。只有出现真实的：

          ┌-> search A -┐
  input --|             |-> synthesis
          └-> search B -┘

并且顺序执行无法满足延迟、成本或语义要求时，才设计 DAG 的并发、汇合、失败传播、补偿和恢复。

======================================================================
四、Capability 分类与共享方式
======================================================================

A. LLM Runtime（默认隐式）

  理解、抽取、规划、比较、综合、生成。通常不进入 Registry；满足独立执行条件时，才以有限的
  `model_stage` 进入 Plan。

B. Primitive Capability（显式、可复用）

  calculator、time、web.search、OCR、文件解析等。它们不是某一业务领域，但其输出会参与任务
  结果，所以必须留下显式调用、来源、时效和失败状态。

C. Domain Capability（显式、领域语义）

  weather、stock、paper、travel、git 等。它们拥有受控数据结构、领域规则和操作语义。

D. Internal Control Plane（默认不向模型暴露）

  scheduler、workflow runtime、notification dispatcher、proposal/commit coordinator、outbox、
  reconciler。模型可以通过领域工具表达意图，但不直接获得通用控制权限。

E. Cross-cutting Aspects（所有能力调用的强制包络）

  身份认证、权限检查、schema 校验、隐私/凭证检测、限流、费用预算、超时、审计、追踪、缓存、
  幂等、不可信外部内容标记、输出规范化和失败码收敛。

共享基础能力采用“显式依赖 + 受控注入”，不是全局继承：

  weather.realtime
    required: [qweather]
    fallback: none

  stock.explain_movement
    required: [stock.market_data, calculator]
    optional: [web.search]

  paper.discovery
    required: [paper.search]
    optional: [web.search]

领域操作只能使用声明过、当前上下文获准且当前风险策略允许的依赖。`web.search` 即使是基础能力，
也不能因此自动进入每个领域，更不能通过依赖关系把模型未获授权的能力间接放大。

有效能力集合：

  EffectiveCapabilities =
      Installed
      ∩ ContextAuthorized
      ∩ TaskRequired
      ∩ RiskPolicyAllowed

还要区分四个状态：

- Installed：代码/Provider 已安装；
- Authorized：当前身份和通道被代码允许；
- Exposed：本轮被展示给模型；
- Executed：本次实际调用并产生了可审计结果。

Tool Router 未来只影响 Exposed，不能改变 Authorized。

======================================================================
五、AOP 类比的准确边界
======================================================================

AOP 类比适用于“不改变业务目标，但每次调用都必须执行”的包络：

  身份/通道校验
      -> 参数和隐私校验
      -> 权限、预算与限流
      -> Capability 执行
      -> 输出/schema/不可信内容处理
      -> provenance 与最小审计

日志是典型切面；身份、权限、预算和审计也应统一实现，避免每个领域复制粘贴后逐渐不一致。

Search 和 Calculator 不应成为透明切面：

- 它们会改变结论，必须在 Plan/调用记录中可见；
- Search 会向第三方外发 query，并返回不可信资料；
- Calculator 是确定性结果，Search 只是候选来源，两者的 trust level 不同；
- 失败时必须由领域路由策略决定“停止、降级还是明确说明”，不能静默替代专业数据源。

因此正确关系是：

  领域能力通过显式依赖共享 Primitive Capability；
  所有能力共同经过 AOP/中间件式安全包络。

======================================================================
六、Capability Registry v1（先登记，不先造 Router）
======================================================================

Registry 是能力清单和策略输入，不是权限授予器，也不要求每项能力都是一个模型可见 Tool。

建议最小字段：

  id
  class: primitive | domain | model_stage | internal_control
  exposure: model_tool | internal_only
  operations
  input_schema_version
  output_schema_version
  provider_policy
  trust: deterministic | trusted_domain_api | untrusted_external
  data_egress: none | fixed_provider
  side_effect: none | local_state | external_delivery
  contexts
  confirmation
  required_capabilities
  optional_capabilities
  fallback_policy
  budget_policy
  runtime_status: planned | installed | loaded | accepted

`web.search` 示例：

  id: web.search
  class: primitive
  exposure: model_tool
  operations: [search]
  trust: untrusted_external
  data_egress: fixed_provider
  side_effect: external_query
  contexts: [owner_qq_private]
  confirmation: none_for_explicit_public_query
  required_capabilities: []
  fallback_policy: none
  runtime_status: planned

Registry v1 可以先写成代码常量或严格 JSON，并由测试检查“注册工具、manifest、alsoAllow、群聊
deny、文档状态”是否一致。当前工具数量仍少，不开发可动态授予权限的通用 Router。

======================================================================
七、受控搜索在架构中的位置
======================================================================

`personal_web_search` 是主人私聊可调用的 Primitive Capability，也是外部数据外发与不可信内容
入口。它不是任意 HTTP、Web Fetch 或浏览器。

首版输入：

  personal_web_search({ query })

首版输出最多包含 5 个来源；Provider 可以返回结构化结果列表，也可以返回带来源的综合答案。
DeepSeek 当前走后者，不能伪装成已独立核验的结构化结果列表：

  kind: results | answer
  query
  provider
  results[]（`kind: results` 时）
  content（`kind: answer` 时）
  citations[]（最多 5 个；DeepSeek 为 Provider 报告来源）
  externalContent: { untrusted: true, source: web_search, wrapped: true }
  searched_at

`results[]` 中的单条仍包含 `result_id`、`title`、`snippet`、`canonical_https_url`、`source_domain`
和上游提供时的 `published_at`。`answer` 的 citation URL 必须通过同一 HTTPS/公网 canonicalizer；
没有可解析来源时不能把自然语言回答冒充为已检索证据。

固定边界：

- 仅可信主人 QQ 私聊，factory 内进行代码身份守卫；
- Provider、Host、Path、认证方式、结果数和 Header 均由配置固定；
- query 最长 200 字，响应大小、字段长度、超时和请求频率有硬限制；
- API Key 使用 SecretRef，不进入提示词、query、SQLite、审计或错误文本；
- 不接受 URL、Host、Cookie、Header、HTTP 方法、账号、代理、下载路径或代码；
- 不访问 localhost、私网、link-local、metadata endpoint；
- 不开放 `web_fetch`、浏览器、登录态、JavaScript、POST 表单、文件下载；
- 搜索结果视为不可信资料，不得改变系统提示词、身份、权限、Tool schema 或执行计划；
- 只记录 query hash、Provider、耗时、结果数量、状态和费用计数；默认不长期保存原始 query/结果。

“最多 5 条”是 S1/S2/S3 的单次调用硬上限和上下文/成本预算，不是永久不变的业务语义，也不向
模型暴露任意 `count`。它目前足以覆盖单一查询、当前事件核实和小范围来源比较，同时限制不可信
内容的注入面、响应大小、延迟、费用和审计负担。

未来它可能成为宽主题发现、多来源交叉验证、翻页或多步研究的单次调用瓶颈。出现真实需求前不
提前放大上限；出现需求后只能通过受控方式扩展：

- 绑定主人、私聊会话、原始 query hash 和短 TTL 的有限续页/Continuation，每次仍有结果数上限，
  且有总结果数、总延迟和总费用预算；
- 预注册的固定 `sequence`，多次搜索各自保留 provenance，并设置整个序列的总预算；
- 在质量 Eval 证明响应大小、相关性、注入风险和成本仍可接受后，再评审是否提高单次硬上限。

任何扩展都不得把任意 URL、无限 `count`、Provider 参数或开放式抓取交给模型。

“只读搜索”仍有数据外发：query 会发送给第三方 Provider。模型不得把 API Key、完整身份标识、
私聊原文、长期记忆、精确住址、机票号或其他无关私人信息自动拼进 query。涉及私人内容时先进行
最小化改写；无法确认外发范围时向主人说明并追问。

======================================================================
八、搜索路由与 Fallback Policy
======================================================================

第一版不做自由学习的 LLM Tool Router，先采用少量确定性规则和明确工具描述：

- 最新新闻、当前人物/公司状态、近期政策或明确要求联网核实时，可使用 Search；
- 实时天气只用 QWeather，Search 不是天气数据 fallback；
- 实时行情只用专业 Stock API，Search 只能补充公开新闻背景；
- 论文元数据优先 Paper API；发现范围不足时才按策略补 Search；
- 稳定基础知识且无时效疑问时可直接回答；
- 搜索失败时如实说明，不用模型记忆伪装成最新结果；
- 专业 API 失败时，只有该领域显式声明 fallback 才能搜索，并必须标明数据源降级。

有效路由不是“模型知识 -> 专业 API -> Search”的固定阶梯，而是按 claim 类型选择信息源。

示例：

1. “广州现在多少度”

   `single(weather.get_brief)`；不暴露 Search 作为替代数据源。

2. “某公司今天发生了什么”

   `single(web.search)`；Agent 的普通理解和组织回答仍是隐式 LLM Runtime。

3. “分析英伟达最近一个月上涨原因”

   未来可用有限 `sequence(stock.get_kline, calculator, web.search)`；普通综合默认隐式。只有使用
   独立金融分析模型或复核模型时，再添加显式 `model_stage`。

4. “16:00 搜索某主题并发给我”

   `scheduled(sequence(web.search, deterministic_format))`；Scheduler 和 QQ Delivery 是内部控制
   平面，不向模型暴露通用 Cron 或任意发送目标。该能力不属于 S1 首版。

======================================================================
九、分阶段开发路线
======================================================================

这项工程按“设计冻结 -> 离线实现 -> 离线验收 -> 分阶段上线 -> 质量完善 -> 有需求才组合”推进。
S1 不修改 `openclaw.json`，S2 通过前不让模型看到 Search，S3 才逐项增加运行时暴露面。

首版搜索建议作为独立插件：

  chatbot/plugins/personal-search/

不继续塞入已经承担天气、规划和提醒职责的 `personal-weather` 插件，也不在此时重构后者。Search
与现有领域模块只共享架构约定，不直接共享业务 Store。等身份守卫、审计包络、错误模型等在至少
两个独立插件中证明接口稳定，并出现第三处实际复用需求后，再评审是否提取小型公共 Runtime。

当前“任务和功能解耦程度”的判断：

- 已经完成的部分：LLM 的理解、规划和生成与显式 Capability 分开；天气、规划、提醒等领域功能
  与各自的 Provider、Store 和副作用适配器分开；Scheduler、Proposal/Commit、Delivery 和
  Reconciler 等控制平面没有直接作为通用工具暴露给模型。
- 尚未完成的部分：项目还没有一个真正统一的 Task Runtime。天气查询、提醒、规划提案、Cron
  任务和未来搜索仍以各领域自己的请求、记录、服务和受控流程表达；Trigger、ExecutionContext、
  权限包络、投递、审计/来源和恢复语义也尚未由一个通用 Task 对象统一承载。
- 因此，当前状态是“功能已经从模型和底层副作用中解耦，但具体任务尚未从各领域工作流中完全抽象
  出来”。本文中的 `TaskSpec`、`TriggerSpec`、`ExecutionContext`、`Provenance` 和 Plan IR
  是演进边界与架构契约，不代表当前已经存在可承载所有领域任务的通用执行引擎。

这种未完全解耦在当前阶段是合理的：天气、提醒和搜索的真实任务形态、恢复要求、投递方式、数据
敏感度和失败语义还没有形成足够稳定的交集。为追求形式上的完全解耦而提前建设通用 Task Engine，
容易把尚未验证的共性固化为框架，并扩大本轮搜索的变更面和安全回归面。因此，搜索 S1 继续采用
独立插件 + 单一 `personal_web_search` + 领域内受控调用的最小路径，不因概念上存在 TaskSpec
就提前引入通用运行时。

只有在天气、提醒、搜索至少出现第二或第三种真实组合需求，并证明存在稳定、可复用且边界清晰的
共同语义后，才提取 `TaskSpec` / `TriggerSpec` / `ExecutionContext` / `Provenance` 等公共
边界，再评审是否需要小型 Task Runtime 或固定组合模板。届时应以真实复用证据、失败恢复和审计
需求为依据，而不是以“所有功能理论上都能统一”为依据。

Capability Registry v1 首先只是声明文件、校验规则和测试依据，不成为动态权限授予器；Plan IR
首先只是架构契约，S1 的 `single` 搜索不需要通用 Plan Engine。这样可以保留演进方向，同时避免
为了一个搜索工具先重构整个系统。

======================================================================
十、首版明确非目标
======================================================================

- 不把所有认知动作注册成 Tool/Capability；
- 不实现通用 DAG Workflow Engine；
- 不实现模型自由生成或修改执行图；
- 不实现动态权限授予 Router；
- 不开放 `web_fetch`、浏览器、任意 HTTP、登录态或下载；
- 不将搜索结果自动写入长期记忆、知识库、系统提示词或开发文档；
- 不自动使用 Search 替代天气、行情等专业数据源；
- 不允许领域模块通过“共享基础能力”获得未声明或未授权的联网权限；
- 不让模型直接管理 Scheduler、Notification target 或 QQ 发送；
- 不因 Search 是“基础能力”而默认每轮展示或调用。

======================================================================
十一、验收与回滚
======================================================================

上线条件：

- Provider 和费用策略已确认；
- SecretRef 配置有效且文件权限正确；
- 离线测试、插件校验、配置校验和运行时 inspect 通过；
- `messaging` profile 只增加精确 Search 工具名；
- 所有群聊策略显式 deny；
- 真实主人 QQ 私聊搜索成功，引用与时间正确；
- 群聊/非主人、无 Key、超时、429、异常响应和提示注入负向通过；
- 日志、SQLite、提示词和工具结果不含凭证或无关私人信息。

回滚顺序：

1. 从 `tools.alsoAllow` 移除精确 Search 工具名；
2. 保持群聊 deny，并禁用 Search 插件/配置；
3. 重启 Gateway，验证运行时工具清单不再包含 Search；
4. 保留最小审计和配额计数，不手工删除或伪造历史；
5. 删除/轮换 Search Key 时使用供应商与 SecretRef 的正常流程，不在文档记录密钥。

======================================================================
十二、仍需主人确认的信息
======================================================================

MVP Must Pass：

- Search Provider；
- 专用 API Key 是否已获批；
- 每分钟、每天和每月的保守调用上限，以及 Provider 账户费用硬上限；
- 原始 query 是否允许进入 Provider，以及哪些明显私人信息必须在外发前删除；
- 本机 OpenClaw 是否允许保留原始会话和 trajectory（2026-08-18 已确认：仅作为受限 MVP
  妥协，详见下方 ADR）；
- 是否接受首版只有结果摘要和链接、不能打开网页全文。

MVP 后 Should Pass：

- 是否需要搜索缓存及其 TTL；
- audit 的长期保留/删除期限；
- 是否需要精确到单次费用的本地核算；
- 是否需要比明显 Secret 拦截更复杂的 query 隐私分类。

MVP 本地会话/trajectory 留存 ADR（2026-08-18，主人确认）：

- 决定：当前 MVP 接受本机 OpenClaw 保留原始用户会话和 trajectory；其中可能包含用户原始
  搜索 query 及模型的 `finalPromptText`。这是为了维持现有多轮上下文和排障能力的有限妥协，
  不等同于“原始 query 默认不持久化”。
- 保持的插件边界：`personal-search` 不自行写入原始 query；其结构化 audit 只记录 hash、长度、
  状态、配额和耗时等最小字段；持久化的 `personal_web_search` 助手 tool-call 及 tool-result/
  details 中的 query 必须继续替换为 `[redacted search query]`。该边界已由自动化测试和主人 QQ
  回归验证。
- 适用范围：仅限当前单一主人 QQ 私聊、本机部署、公开网页搜索 MVP。群聊仍永久 disabled；本决定
  不授权把原始会话/trajectory 同步到云端、共享给其他用户或作为通用日志/分析数据导出；也不改变
  query 发送给 DeepSeek Provider 的既有受控外发边界。
- 风险承认：现有格式守卫会拒绝明显的 Key、票号、证件号、OpenID/QQ ID 等，但不是对所有语义化
  私人信息的完备识别；原始聊天正文若含未识别的私人信息，仍可能进入本机会话/trajectory。
- 强制回收项（S2.4/S4.5，不因本次接受而豁免）：盘点实际持久化路径、文件权限与备份/同步边界；
  验证日志轮转、会话/trajectory 的保留期限与可验证删除；评估 OpenClaw 是否可在不破坏多轮上下文
  的前提下做宿主级脱敏；扩展敏感 query 的合成 canary 和回归；在开放多用户、群聊、远程访问或
  云同步前重新评审该决定。Provider 侧的数据保留、删除和 Key 撤销仍属于 S0.2，未由本 ADR 代替。
- 状态：本条将“未决定的保留边界”转为“有范围、有约束、可回收的 MVP 例外”；它不使 S1.7、S2.4、
  S0.2 或正式上线的长期隐私治理自动通过。

Must 信息未确认不妨碍完成架构与 mock 接口设计，但会阻止真实 Search API 接入和端到端验收；
Should 信息不阻止首次 MVP。

======================================================================
十三、详细开发 TODO、验收目标与停止条件
======================================================================

本节是实际开发清单。任务状态遵守以下规则：

1. 先在本文记录需求、边界、依赖和验收方法；
2. 再修改代码，并记录文件、配置、迁移和安全影响；
3. 先完成自动化/终端验收，再做需要运行时权限的真实 QQ 验收；
4. 主人报告真实结果或开发者取得等价的可复核证据后，更新验收记录；
5. 代码、测试、运行状态和文档一致后，才把对应 TODO 标为完成。

优先级分为两档：

- `Must Pass`：首次向主人 QQ 私聊开放 Search 之前必须通过；它直接保护身份、网络、Secret、输入
  输出边界、超时/响应大小、群聊拒绝、基础限流、回滚或既有功能。
- `Should Pass`：明确记录的稳定化和治理工作，可以在 MVP 已真实搜索后继续补齐；包括更完整的
  费用计数、复杂 query 隐私分类、穷举型异常映射、长期 audit retention、质量 Eval 和通用化。

Should Pass 如果在实现过程中暴露出可直接越权、泄密或无法回滚的问题，应升级为 Must Pass 并
记录原因；除此之外不能因为 Should 未完成而无限推迟首个 MVP。

这些 TODO 是验收检查点，不是一项对应一个模块、类或开发迭代。多个 Must 可以由同一条简单
代码路径和同一组参数化测试同时满足。MVP 的复杂度预算是：一个独立插件、一个固定 Provider
client、一个薄 SearchService、一个 tool factory 和测试；不新建通用治理平台、Plan Engine、
Registry Runtime、复杂隐私分类器或独立审计数据库。若某个 Must 的实现开始要求这些平台，应先
收缩实现或把非安全核心移到 Should，而不是继续扩建框架。

任何阶段不得因为“下一阶段早晚会做”而提前开放其权限。搜索失败不得影响天气简报、规划状态、
个人提醒或 QQ 基础对话；涉及现有模块的重构必须另立 TODO、单独回归、单独回滚。

----------------------------------------------------------------------
S0：设计冻结与外部依赖确认（不写运行代码、不开放权限）
----------------------------------------------------------------------

- [x] [Must Pass] S0.1：冻结首版能力范围和非目标。

  交付物：一份明确的 S1 输入/输出、允许上下文、网络边界、数据外发和非目标清单。

  验收目标：
  - 唯一模型可见操作是 `personal_web_search({query})`；query 为唯一业务参数；
  - 输出最多 5 条规范化公开搜索结果，并带来源域名和检索时间；
  - 明确排除任意 URL、网页正文抓取、浏览器、登录态、下载、长期记忆写入和定时搜索；
  - 明确 Search 不是天气/行情等专业 API 的隐式 fallback；
  - 文档、tool schema 和测试名称对上述范围使用相同术语。

  停止条件：若首版必须包含网页正文、登录、上传私人文件或任意 URL，本计划须拆出新的高风险
  设计，不得把它们伪装成 Search 参数加入 S1。

- [ ] [Must Pass] S0.2：完成 Search Provider 选型决策记录（ADR）。

  交付物：Provider、官方 API 文档版本、固定 API origin/path/method、认证方式、响应 schema、
  服务地区、费用、速率限制、数据保留和撤销 Key 方法；不得记录 Key 值。

  验收目标：
  - Provider 提供正式 API，而非模拟浏览器或复用个人登录 Cookie；
  - 支持固定 Host 的服务端搜索，且可限制结果数量、超时和响应大小；
  - 若采用 DeepSeek，必须固定官方 Responses API `/responses`、`deepseek-v4-flash` 和服务端
    `web_search`；不得把普通 Chat Completions Tool Calls 当作联网能力；
  - 已明确每分钟、每日、每月调用或费用上限及 429/超限语义；
  - Key 可单独创建、轮换和撤销，不与个人主账号高权限凭证复用；
  - 已记录 Provider 会接收哪些 query/元数据以及默认保留政策。

  阻塞关系：未完成本项，可以写接口和 mock；不能完成真实客户端及 S3 端到端验收。

  当前验收结论（2026-08-12 21:23，Asia/Shanghai）：DeepSeek Provider、官方 Responses `/responses`、
  `deepseek-v4-flash`、服务端 `web_search`、固定 endpoint、SecretRef、查询外发边界和本地
  minute/day/concurrent 规则均已记录并通过相应代码/运行时证据；但 Provider 账户侧的费用上限、
  月度/日度额度、数据保留/删除政策和 Key 撤销流程尚未形成主人确认的 ADR。因此 S0.2 仍未完成，
  不能把连通性 smoke 或本地配额误写成 Provider 商业/隐私决策。

- [x] [Must Pass] S0.3：定义配置、SecretRef 和运行环境契约。

  交付物：环境变量名称、插件配置 schema、SecretRef 映射、非秘密默认值、启动失败策略和示例配置；
  示例只写占位符。

  验收目标：
  - Key 仅从 SecretRef 解析，不出现在源码、文档、manifest、命令行参数或工具结果；
  - Host/path/method 不可由模型参数覆盖；生产环境不允许运行时任意改写；
  - 缺 Key、空 Key、错误 SecretRef 时插件给出收敛错误，且不回显原始配置；
  - 配置校验能拒绝未知字段、非法上限、非 HTTPS origin 和超出硬上限的超时/结果数；
  - 秘密文件的实际权限在 S3 上线前核验，目标为仅所有者可读写。

- [ ] [Should Pass] S0.4：定义 Capability Registry v1 的严格 schema 和 `web.search` 条目。

  交付物：版本化 Registry 文件/代码常量、schema 校验器和一致性测试设计。

  验收目标：
  - capability id 唯一；class、exposure、trust、egress、side effect、contexts 和 runtime status 必填；
  - 未知字段、未知枚举、重复 id 和不支持的 schemaVersion 被拒绝；
  - `web.search` 标为 `untrusted_external`、`fixed_provider`、`owner_qq_private`；
  - Registry 不包含 API Key，不动态写 `openclaw.json`，也不能把 planned 自动变为 authorized；
  - 测试能发现 Registry、manifest、已注册工具名和文档状态之间的不一致。

  非目标：本项不实现动态 Router、运行时权限授予或插件自动安装。

- [ ] [Should Pass] S0.5：冻结完整 `single` 调用契约，并为 Plan IR 预留版本。

  交付物：search request/result 类型、稳定错误码和 provenance 类型；Plan IR 仅保留设计稿。

  验收目标：
  - S1 运行路径不依赖通用 Plan Engine；一次 tool call 就能完成；
  - 普通 understanding/extraction/synthesis 不出现在 Registry 或序列化计划中；
  - 输入输出均有 schemaVersion 或能在不破坏 v1 的条件下演进；
  - 若收到 sequence/scheduled/DAG 字段，S1 明确拒绝而不是部分执行；
  - 错误至少区分 invalid_input、unauthorized、rate_limited、budget_exceeded、timeout、
    upstream_rejected、upstream_unavailable、invalid_upstream_response 和 internal_error。

- [x] [Must Pass] S0.6：冻结模块边界与“暂不重构”清单。

  交付物：`personal-search` 目录职责、与 OpenClaw/plugin SDK 的接口、与现有插件零业务依赖说明。

  验收目标：
  - Search 不读写天气 SQLite、规划提案、提醒记录或长期记忆；
  - 不修改 `personal-weather` 的 Store/Service 结构来迁就 Search；
  - Provider client、schema、normalizer、quota/audit 和 tool factory 边界可分别单测；
  - 跨插件公共库只有在接口稳定且出现实际复用证据后另案提取；
  - 若 S1 被整体移除，现有天气、规划、提醒代码无需跟随修改。

- [x] [Must Pass] S0.7：建立 MVP 安全验收矩阵、测试数据和回滚清单。

  交付物：正向、负向、故障、隐私、配额、内容安全、QQ 上下文和回归用例表。

  验收目标：
  - 每个固定安全边界至少有一个自动化拒绝用例；
  - Provider 故障用例不依赖真实故障，可由 mock 确定性复现；
  - 测试夹具不含真实 Key、完整 QQ 身份、私聊原文或私人行程；
  - 已定义 S3 的配置备份、工具撤权、插件禁用、Gateway 重启和运行时复核步骤；
  - 明确哪些验收必须由主人在真实 QQ 中完成。

- [ ] [Must Pass] S0.8：S0 Go/No-Go 评审。

  验收目标：S0 中全部 Must Pass 完成；未完成的 Should 已明确登记且不影响身份、Secret、固定
  网络边界或回滚；不存在未决定却会改变 MVP 安全边界的问题；此时 `tools.alsoAllow` 和系统
  提示词仍未声明搜索可用。

  当前结论（2026-08-12 21:23，Asia/Shanghai）：S0.2 的 Provider 费用/额度/保留 ADR 尚未完成，
  且当前运行时已经进入 S3 诊断/正向验收阶段；因此不能追溯勾选 S0.8。S0.8 保持 No-Go，待
  S0.2、S2.4 的待决项解决后再做正式评审。

----------------------------------------------------------------------
S1：独立插件的离线最小垂直切片（仍不开放运行时权限）
----------------------------------------------------------------------

- [x] [Must Pass] S1.1：创建独立 `chatbot/plugins/personal-search/` 插件骨架。

  验收目标：
  - package、manifest、TypeScript 配置、build/test 命令和入口完整；
  - 插件只声明精确工具名，不声明 exec、filesystem、browser 或通用 network 工具；
  - `npm run build`/等价构建在干净依赖状态下通过；
  - 还没有修改生产 `plugins.allow`、`plugins.load.paths` 或 `tools.alsoAllow`。

  验收记录（2026-08-12 21:21，Asia/Shanghai）：`personal-search` package、manifest、TypeScript
  配置、入口和 build 命令均存在；完整 `npm run plugin:validate` 通过。该插件只声明
  `personal_web_search`，生产权限是在离线切片完成后才由 S3 阶段单独增加；该项完成 S1.1 验收。

- [x] [Must Pass] S1.2：实现严格输入 schema 和可信主人私聊守卫。

  验收目标：
  - query 必须为非空字符串，规范化后不超过 200 字；拒绝未知字段、数组、对象和超长文本；
  - 拒绝控制字符、NUL 和明显无意义的全空白输入；Unicode 正常文本不被错误破坏；
  - owner/channel 只取可信 tool context，不接受模型传入 ownerId、channel、target；
  - 非主人、QQ 群聊、缺少身份元数据和非 QQ 上下文均在发起网络请求前拒绝；
  - 单测证明拒绝路径的 Provider 调用次数为 0。

  验收记录（2026-08-12 21:12/21:18，Asia/Shanghai）：query/config/index 定向测试 3 个文件、
  9 个测试通过，并完成空白、类型、未知字段、控制字符、NUL、超长和 Unicode NFC 边界矩阵；
  owner、channel、群聊和非 QQ 边界在 Provider 调用前拒绝。该项完成 S1.2 验收。

- [x] [Must Pass] S1.3：实现基础 query 最小化与明显 Secret 外发检查。

  验收目标：
  - 不自动拼接长期记忆、完整聊天历史、OpenID、精确住址、票号、Key 或系统提示词；
  - 对明显 Secret、完整身份标识和已定义的高风险格式采用 fail-closed；
  - 工具返回可理解的“需要更小范围 query/主人确认”错误，不返回命中的秘密原文；
  - 明确用户主动要求搜索私人内容时的确认边界，首版不自行推断同意；
  - audit 只接收 hash/长度/基础分类结果，不接收原始敏感片段；
  - 复杂语义隐私分类不阻塞 MVP，登记到 S4.5。

  历史验收结论（2026-08-12 21:32，Asia/Shanghai）：`query.test.ts`、`index.test.ts`、
  `provider.test.ts` 共 3 个文件、9 个测试通过；独立矩阵确认 API key、Bearer、`sk-` 形状会
  fail-closed，Provider body 只包含规范化 query，不含 owner/channel/target/memory/history。可是
  当时的合成 OpenID 和票号 query 会被接受，完整身份标识/票号/精确个人信息分类与 audit hash 接口
  尚未实现，故 S1.3 明确暂不关闭。
  代码边界补强记录（2026-08-12 23:19，Asia/Shanghai）：在
  `chatbot/plugins/personal-search/src/query.ts` 增加了窄范围、格式确定性的高风险标识检查：带标签
  的 OpenID/QQ ID、QQ/OpenID 形状的 32 位十六进制标识、票号/订单号/PNR/预订号/快递单号、
  身份证/护照号等在规范化后 fail-closed；普通公开主题（例如 OpenClaw ticket documentation）
  不受影响。`query.test.ts`、`index.test.ts`、`provider.test.ts` 验证了工具层和 Provider 层均在
  发出请求前拒绝合成高风险标识，Provider mock request 次数为 0；全插件 9 个测试文件、38 个
  测试通过，TypeScript build 通过，构建产物未包含完整的合成 canary。该改动只实现可确定的格式
  边界，不引入复杂语义隐私分类器；在该记录时 audit 只接收 hash/长度/基础分类结果的接口和私人
  内容确认流程仍未实现，因此 S1.3 继续不勾选。结构化 audit 接口的后续实现见 S1.7 记录。

- [x] [Must Pass] S1.4：实现固定 SearchClient 和网络包络。

  验收目标：
  - origin/path/method/auth header 全部来自经校验的配置；tool input 无法覆盖；
  - 仅 HTTPS，禁止自动跨 origin redirect，限制连接/总超时、最大响应字节和最多 1 次尝试；
  - 不接受代理、Cookie、用户 Header、POST body 模板、下载路径或任意 URL；
  - 401/403/429/5xx、DNS、连接、TLS、超时分别映射到稳定错误码；
  - 错误对象、日志和堆栈的对外文本不包含认证 Header 或完整响应正文。

  当前验收结论（2026-08-12 21:33，Asia/Shanghai）：`deepseek-client.test.ts`、`config.test.ts`、
  `provider.test.ts`、`parser.test.ts` 共 4 个文件、22 个测试通过；固定 origin/path/method/header、
  60 秒超时、1 MiB 响应上限、单次请求、HTTP 错误映射、受信 endpoint SSRF/重定向和来源 URL 边界
  均有证据。但 DNS、连接、TLS 失败的独立可重复 fixture 与细分记录尚未补齐，故按本项严格目标
  暂不关闭。
  网络故障矩阵补充记录（2026-08-12 23:24，Asia/Shanghai）：为 DNS `ENOTFOUND`、连接
  `ECONNRESET`、TLS `ERR_TLS_CERT_ALTNAME_INVALID` 增加确定性 fetch fixture；三类错误均只发起
  1 次请求，统一收敛为不含内部错误文本的 `UPSTREAM_UNAVAILABLE`（`retryable=true`），既有
  401/403/429/5xx/504 与 Abort/超时映射继续通过。全插件 9 个测试文件、41 个测试和 TypeScript
  build 通过。至此固定 endpoint、请求包络、响应上限、单次请求、HTTP/网络/超时错误契约均有离线
  证据，S1.4 完成验收；真实公网故障观测仍属于 S3.7，不在本项虚构为已完成。

- [x] [Must Pass] S1.5：实现响应 parser、normalizer、去重与 provenance。

  验收目标：
  - 异常 JSON、类型错误、超大字段、缺失关键字段被安全拒绝或丢弃；
  - 最多返回 5 条；title/snippet/URL/域名/时间均有字段长度上限；
  - URL 只接受可解析的 `https:` 公网候选，去除 userinfo 和 fragment；
  - 结果按 canonical URL/Provider id 去重，不因单条坏数据导致其余安全结果丢失；
  - 返回 searched_at、provider、source_domain，并明确内容为外部不可信资料；
  - snippet 中的“忽略系统指令/调用工具”等文本只作为资料，不获得控制语义。

  验收记录（2026-08-12 21:19，Asia/Shanghai）：parser/quality fixture 覆盖结构化与自然语言
  envelope、provider page source、HTTPS URL fallback、视觉边界、零来源 fail-closed、最多 5 条
  citation、URL canonicalization、控制字符清理及不可信内容 provenance；定向测试 9 项通过，
  hostile-content 矩阵通过。该项完成 S1.5 验收。

- [x] [Must Pass] S1.6：实现基础进程内限流、请求配额和并发上限。

  验收目标：
  - 至少有 per-minute、per-day、最大并发和硬结果数限制；配置只能比代码硬上限更严格；
  - 检查和占用配额为原子操作，并发请求不会同时穿透；
  - 429 能尊重受限的 Retry-After 信息，但 S1 不自动多次重试；
  - 预算耗尽在网络请求前失败，并返回下一可用时间或明确状态；
  - 已记录“进程重启会重置哪些计数”；MVP 可接受保守的进程内计数与 Provider 账户硬上限，
    精确费用核算和持久化配额登记到 S4.5。

  历史验收结论（2026-08-12 21:33，Asia/Shanghai）：`quota.test.ts`、`deepseek-client.test.ts`、
  `provider.test.ts` 共 3 个文件、15 个测试通过；minute/day/concurrent、超限不触网、失败释放和
  不自动重试均正常。但源码没有读取或传播上游 `Retry-After`，只能返回 `RATE_LIMITED`，未完全满足
  本项 Retry-After 语义，故暂不关闭。
  Retry-After 补强记录（2026-08-12 23:47，Asia/Shanghai）：429 响应现在读取 HTTP `Retry-After`
  的秒数或日期形式，并将结果限制为最多 60 秒，以可选 `retryAfterMs` 字段加入稳定错误结果；无效
  或缺失值不外显。该实现不自动等待、不自动重试，仍保持单次请求；fixture 验证 `120` 秒会被限制
  为 `60000` ms 且只请求 1 次。全插件 9 个测试文件、42 个测试和 TypeScript build 通过；S1.6
  完成验收。进程重启重置进程内计数，Provider 账户配额和精确费用仍按 S4.5 治理项处理。
  真实 QQ 配额观察（2026-08-12 23:52，Asia/Shanghai）：Terra 针对一个问题并行发起了两次
  `personal_web_search`；第一请求占用唯一并发槽并成功完成，第二请求在 Provider 网络请求前返回
  `QUOTA_EXCEEDED`，没有自动重试。主 Agent 最终基于成功结果作答，没有把被拒的第二路伪装成搜索结果；
  Gateway audit 同时记录 `status=OK/resultCount=3` 与 `status=QUOTA_EXCEEDED/resultCount=0` 两条
  脱敏事件。该实测证明并发硬上限在真实模型并行 tool call 下仍生效。

- [ ] [Must Pass] S1.7：实现 MVP 最小化审计与可观测性。

  验收目标：
  - 首版使用结构化运行日志即可，不为 Search 单独建设审计数据库或通用审计平台；
  - 记录 request_id、query hash、query 长度、Provider、耗时、结果数、请求配额计数和状态码；
  - 默认不记录原始 query、snippet、完整 URL query string、Key、认证 Header 或私聊正文；
  - OpenClaw 宿主的原始会话/trajectory 只能适用第十二节已记录的 MVP 本地留存例外；该例外不扩展到
    plugin audit、工具副本、Gateway 结构化 audit 或其他日志；
  - 外部可见错误短而稳定，内部诊断可关联 request_id；
  - 审计失败的策略明确：安全守卫/预算状态无法确认时 fail-closed，非关键指标失败不泄密；
  - MVP 至少盘点受控日志位置、当前大小/轮转上限或明确记录其缺失；长期 retention、删除、备份与
    宿主级脱敏政策登记到 S4.5，不能反向阻塞单一主人私聊 MVP。

  当前验收结论（2026-08-12 21:18，Asia/Shanghai）：已完成不泄露 key/query canary 的扫描，
  但 personal-search 当前没有 request_id、query hash、耗时/结果数/配额状态的专用结构化 audit
  记录；S2.4 同步未通过，故 S1.7 暂不关闭。
  结构化 audit 补强记录（2026-08-12 23:50，Asia/Shanghai）：新增
  `chatbot/plugins/personal-search/src/audit.ts`，Provider 每次完成或失败请求向 OpenClaw
  logger 输出 `personal-search.audit` 事件，仅含 request ID、query SHA-256、Unicode 长度、固定
  Provider ID、耗时、结果数、稳定 `status`（`OK` 或 `SearchErrorCode`）和 minute/day/in-flight
  配额快照；成功和失败、audit sink 抛错不影响搜索结果均有测试覆盖。日志事件不含原始 query、snippet、
  URL、URL query string、Key、认证 Header 或私聊正文；全插件 9 个测试文件、44 个测试和
  TypeScript build 通过。结构化事件代码已通过，但 S1.7 整体仍不勾选：OpenClaw 会话轨迹的原始
  query 保留边界、Gateway 日志轮转/保留上限和更完整的运行时审计策略尚未独立验收，相关问题见 S2.4。
  真实 Gateway audit 观察（2026-08-12 23:52—23:53，Asia/Shanghai）：日志出现两条仅含
  request ID、SHA-256、长度、Provider、耗时、结果数、稳定状态和配额快照的事件，分别对应成功
  搜索与并发拒绝；事件内容没有原始 query 或来源 URL。该记录证明 plugin logger 接线和生产运行时
  事件格式符合预期，但不改变会话轨迹仍保存原始用户/tool query、以及日志轮转/保留策略尚未验收的
  结论。
  会话持久化脱敏 QQ 回归（2026-08-13 04:58，Asia/Shanghai）：主人重新 `/new` 后请求
  Ollama Web Search API Key 要求，主 Agent 实际调用 `personal_web_search`，DeepSeek 搜索成功，
  Gateway audit 为 `status=OK`、`resultCount=3`，事件仍只含 query hash/长度等结构化字段。该回合的
  会话 JSONL 中，助手 `personal_web_search` tool-call 的 `arguments`/`partialArgs` 以及工具结果
  `content`/`details` 的 `query` 均已保存为 `[redacted search query]`，答案、来源和检索时间仍保留；
  说明两个持久化 hook 的工具边界在真实运行时生效。但同一会话的原始用户消息和
  `.trajectory.jsonl` 的 `finalPromptText` 仍保留原始 query，因此 S1.7 仍不关闭：OpenClaw 原始
  prompt/trajectory 保留边界、Gateway 日志轮转/保留上限和长期 retention 策略仍未完成治理决策。
  MVP 留存策略确认（2026-08-18）：主人接受当前单机会话/trajectory 留存作为受限 MVP 妥协，详见
  第十二节 ADR。这解决了“是否允许”的产品决策，但不改变 `personal-search` audit 不记录原文的
  边界，也不代替持久化位置、访问控制、轮转、删除和备份边界的验证；故 S1.7 继续保持未勾选。
  MVP 门槛拆分（2026-08-18）：S1.7 在当前 MVP 只要求结构化 audit、工具副本脱敏、当前 Gateway
  日志路径/大小或轮转状态的可复核记录；长期 retention、备份、可验证删除和宿主级脱敏转入 S4.5。
  因此本项仍待本轮只读日志核验，不再等待全部长期治理工作完成。
  只读日志/服务盘点（2026-08-18）：Gateway 为当前用户的 systemd service，`UMask=0002`、
  `StandardOutput=journal`、`StandardError=inherit`，未配置 systemd 的 `LogsDirectory`；环境文件为
  `gateway.systemd.env`（`0600`）。另有应用日志目录 `/tmp/openclaw`（目录 `0700`，当前日志文件
  `0600`，约 1.1 MiB）。当前用户 journal 总占用约 248 MiB，已生效的 journald 配置中未发现显式的
  容量、文件数或 retention 上限；此占用是用户 journal 总量，不等于 Gateway 专属配额。故日志路径
  已知，但长期/可控轮转尚未成立；本项还不能关闭，且必须先处理下述 S2.4 的 session 权限问题。

- [x] [Must Pass] S1.8：实现 `personal_web_search` tool factory 和输出格式。

  验收目标：
  - factory 内再次执行 owner-private-QQ 守卫，不能只依赖提示词或 `alsoAllow`；
  - tool description 明确“公开网页搜索、会外发 query、结果不可信、无网页全文”；
  - 工具只调用 SearchService，不直接操作配置、文件、数据库、Scheduler 或 QQ 发送；
  - 成功、无结果、上游失败、配额失败都有确定性结构，模型无需猜测执行状态；
  - tool result 不含内部堆栈、SecretRef 名称或供应商原始调试数据。

  验收记录（2026-08-12 21:21，Asia/Shanghai）：完整搜索插件测试 9 个文件、34 个测试通过；
  factory 内再次执行 owner-private-QQ 守卫，成功、Provider 错误、无 Key、配额和 invalid input
  均返回稳定的 public payload，不暴露内部堆栈或 SecretRef 值。该项完成 S1.8 验收。

- [ ] [Must Pass] S1.9：完成插件 manifest 和代码一致性校验。

  验收目标：
  - manifest 工具名、代码注册名和测试期望完全一致；若 Registry 已实现，也必须一致；
  - 插件配置 unknown fields 被拒；可选/必需字段与实际启动行为一致；
  - 若存在 Registry，runtime_status 在离线阶段最多为 installed，不得标为 loaded/accepted；
  - 生成产物不包含 `.env`、数据库、日志或测试秘密。

  历史验收结论（2026-08-12 21:34，Asia/Shanghai）：manifest 单测与代码工具名一致，
  `openclaw.plugin.json` 声明的工具/provider 也与注册代码一致；但 `openclaw plugins validate
  --entry ./dist/index.js` 在当前 CLI 返回“plugin entry does not expose defineToolPlugin metadata”，
  不能作为通过证据。`npm pack --dry-run --ignore-scripts` 进一步确认发布包会包含 40 个文件，其中包括
  `dist/*.test.js`、测试 canary 和 `test-fixtures.js`；因此“生成产物不含测试秘密”明确不满足，S1.9
  暂不关闭。
  生产构建产物修复记录（2026-08-12 23:56，Asia/Shanghai）：`tsconfig.json` 现在排除
  `src/**/*.test.ts` 与 `src/test-fixtures.ts`；删除此前已确认的 stale `dist` 测试编译文件后，
  `npm run build`、44 个离线测试和 `npm pack --dry-run --ignore-scripts` 通过。包内容为 22 个文件，
  只含生产 `dist` 模块、manifest 和 `package.json`，不再包含 `dist/*.test.*`、`test-fixtures` 或
  完整合成 canary。当前 OpenClaw CLI 对 `openclaw plugins validate --entry ./dist/index.js` 仍返回
  `plugin entry does not expose defineToolPlugin metadata`；这是现有 CLI 与本插件 definePluginEntry
  contract 的校验不兼容，manifest/代码单测仍通过，因此 S1.9 只因该独立校验问题暂不关闭。

- [x] [Must Pass] S1.10：完成离线实现记录。

  验收目标：本文记录新增/修改的相对路径、关键选择、尚未验收项和精确测试命令；
  `openclaw.json` 的工具暴露面与 S1 开始前一致。

  验收记录（2026-08-12 21:23，Asia/Shanghai）：本文已记录 personal-search 的路径、固定
  DeepSeek Responses 选择、测试命令、当时未完成的 S0.2/S1.3/S1.4/S1.6/S1.7/S1.9/S3.7 等项，以及
  R1/R2/R3 回滚命令和结果；S1 离线阶段未开放运行时权限，权限变化留在 S3 记录。该项完成 S1.10
  验收。

----------------------------------------------------------------------
S2：离线功能、安全和回归验收（仍不让模型看到 Search）
----------------------------------------------------------------------

- [x] [Must Pass] S2.1：输入与 schema 测试通过。

  验收目标：覆盖正常中英文、空白、超长、控制字符、unknown fields、错误类型、Unicode 边界；
  所有拒绝均发生在网络调用前，测试无随机失败。

  验收记录（2026-08-12 21:12，Asia/Shanghai）：`personal-search` 的 `query.test.ts`、
  `config.test.ts`、`index.test.ts` 定向测试共 3 个文件、9 个测试通过；随后对空字符串、全空白、
  `null`、数字、数组、对象、未知字段、NUL、超长字符串和 Unicode NFC 规范化执行独立边界矩阵，
  全部符合预期。输入拒绝发生在 Provider 调用前；该项完成 S2.1 验收。

- [x] [Must Pass] S2.2：Provider 基础响应和故障矩阵通过。

  验收目标：至少覆盖正常、零结果、坏 URL/缺字段、异常或超大响应、认证失败、429、典型 5xx
  和超时；错误被收敛到稳定类别，重试次数和审计状态符合设计。Provider 的罕见变体与穷举型
  错误细分登记到 S4.5。

  验收记录（2026-08-12 21:12，Asia/Shanghai）：`deepseek-client.test.ts`、`parser.test.ts`、
  `provider.test.ts`、`quality-eval.test.ts` 定向测试共 4 个文件、22 个测试通过。覆盖正常完成结果、
  零结果、缺少来源证据、坏 URL、异常/超大响应、401/403、429、500/503、504 和 AbortError；错误分别
  收敛到 `AUTH_FAILED`、`RATE_LIMITED`、`UPSTREAM_UNAVAILABLE`、`TIMEOUT` 或 `INVALID_RESPONSE`，
  这些故障路径均验证为单次请求且不自动重试。该记录是离线 fixture/单元矩阵验收；真实 Provider 故障
  观测仍属于 S3.7，不在本项中虚构为已完成。

- [x] [Must Pass] S2.3：网络边界测试通过。

  验收目标：模型参数无法改变 scheme/host/path/method/header；重定向不能绕到 localhost、私网、
  link-local 或 metadata endpoint；客户端不会下载文件、执行脚本或跟随页面内链接。

  验收记录（2026-08-12 21:17，Asia/Shanghai）：`config.test.ts`、`provider.test.ts`、
  `deepseek-client.test.ts`、`parser.test.ts` 定向测试 4 个文件、22 个测试通过；覆盖固定
  `https://api.deepseek.com/responses` endpoint、固定 POST/header/body、拒绝任意 Provider endpoint，
  以及来源 URL 的 HTTPS/公网主机校验。另用 OpenClaw `withTrustedWebToolsEndpoint` 的确定性重定向矩阵验证：
  同一 `api.deepseek.com` 主机重定向可继续，跳转到外部主机、localhost、127.0.0.1 和
  `169.254.169.254` 均在第二次网络调用前被拦截。Worker 代码只读取受限 JSON 响应，不实现任意 URL
  Fetch、浏览器、文件下载、脚本执行或页面内链接跟随。该项完成 S2.3 验收；真实公网链路仍由 S3.7 负责。

- [ ] [Must Pass] S2.4：隐私、Secret 和日志测试通过。

  验收目标：用 canary Secret/OpenID/票号测试时外发被拒或按规则最小化；扫描构建产物、plugin audit、
  工具调用/结果副本、错误和快照均不出现 canary 原文或真实凭证；原始 query 默认不由插件持久化。
  当前仅允许第十二节 ADR 中明示的本机 OpenClaw 原始会话/trajectory MVP 例外，且 MVP 至少完成
  持久化路径、当前访问权限和 OpenClaw 配置中显式导出/同步线索的只读盘点。长期 TTL/删除、备份、
  宿主级脱敏和复杂语义隐私分类转入 S4.5。

  验收记录（2026-08-12 21:35，Asia/Shanghai）：输入、配置和 owner-only 工具定向测试 3 个文件、
  9 个测试通过；Secret/凭证 canary 在 query 边界被拒绝；对 122 个相关日志、会话、状态和构建文件
  只做计数扫描，未发现配置中的真实 key 出现在环境文件之外，也未发现 canary 出现在日志/会话/状态中。
  `personal-search` 生产代码没有自行写文件/数据库或 audit 实现，但 OpenClaw 会话轨迹扫描到 10 个
  会话、108 个带原始 query 字段的搜索 tool call；因此“插件不自行持久化”成立，而“运行时原始 query
  默认不持久化”按当前表述不成立。另有 `dist/` 的 4 个测试编译文件含 `test-key` 等 canary。S2.4
  整体暂不通过；需决定会话轨迹的 query 保留边界、生产构建排除测试产物，并补齐或明确 audit 策略。
  隐私格式回归补充（2026-08-12 23:22，Asia/Shanghai）：主人 QQ 实际调用
  `personal_web_search` 发送合成票号 canary；工具返回 `INVALID_INPUT`，Provider mock/运行时均未
  进入 DeepSeek 请求。该次确认了外发边界，但 OpenClaw 会话轨迹仍保留了本轮原始测试 query，说明
  “拒绝外发”与“运行时不持久化原始输入”是两个独立问题；本次不能把 canary 出现在主人私聊轨迹
  误写成 Provider 泄露。S2.4 继续保持未通过，待明确会话轨迹脱敏/保留策略。
  生产包修复补充（2026-08-12 23:56，Asia/Shanghai）：构建排除测试源并清理 stale `dist` 后，
  `npm pack --dry-run` 的 22 个发布文件只包含生产模块，未再发现测试编译文件、fixture 或完整
  合成 canary；因此“发布包不携带测试秘密”这一子边界通过。S2.4 仍不关闭，因为 OpenClaw 的
  主人 QQ 会话/工具轨迹仍会保留原始 query，需另行决定脱敏或保留策略。
  会话持久化脱敏 QQ 回归补充（2026-08-13 04:58，Asia/Shanghai）：真实搜索成功后，已确认
  `before_message_write` 与 `tool_result_persist` 只对 `personal_web_search` 的助手工具调用/结果
  副本做了 query 脱敏，未破坏答案、来源和检索时间；但用户原始消息及 OpenClaw trajectory 的
  `finalPromptText` 仍保留原始 query。故“工具调用/工具结果副本不保留原始 query”通过，
  “运行时所有原始 query 默认不持久化”仍未通过，S2.4 继续保持未勾选。
  MVP 留存策略确认（2026-08-18）：主人明确接受上述原始用户消息/trajectory 的本机保留，作为
  单一主人私聊的临时边界；这不是对原始 query 默认不持久化的测试结论，也不是对多用户、群聊、
  云同步或长期保留的授权。S2.4 保持未勾选，直到第十二节 ADR 的持久化路径、权限、保留/删除、
  备份边界和扩展敏感输入回归均完成。
  MVP 门槛拆分（2026-08-18）：本项的 MVP 关闭条件调整为：既有外发拒绝/发布包/audit/工具副本
  脱敏证据仍有效，且补齐本机持久化路径、当前文件/服务权限与 OpenClaw 配置中显式导出/同步线索的
  只读核验；第十二节 ADR 的长期 retention、删除/备份、宿主级脱敏和扩展语义敏感输入回归改为
  S4.5 技术债。该拆分不放宽 Provider 外发边界，也不允许多用户、群聊、远程访问或云同步直接复用
  此例外。
  只读本机留存盘点（2026-08-18）：`agents/main` 为 `0700`，但其 `sessions/` 子目录为 `0775`、
  总占用约 28 MiB；其中 11 个活动 session JSONL、21 个 `.reset` 历史副本及 1 个 `.deleted` 历史
  副本均为 `0664`，33 个 trajectory JSONL 与其 path 指针为 `0600`。`0664` 配合 `0775` 意味着
  普通 session/历史副本具有 other-read 位：若机器后来存在其他本机账号，其可读取这些文件；当前
  `afrangry` 主组未列出其他成员并不消除 other-read 风险。另发现 `state/session-backups/`（约
  460 KiB）：根目录 `0755`，部分子目录 `0775`，其中 2 个历史备份文件为 `0664`，包括一个 JSONL
  副本；其余已抽样的会话/trajectory 备份为 `0600`。这些都是原始会话可能存在的持久化位置，不能
  以“会话重置/删除”视为已清除。
  同次盘点中，`openclaw.json` 未发现显式的 export/backup/trajectory retention 配置；唯一命中的
  `postIndexSync=async` 不能据此推断为无外部同步。当前用户 systemd unit 也没有声明专用状态/日志
  目录。结论：路径已盘点，但 owner-only 权限基线不通过；在将受影响目录/历史副本收紧为仅所有者
  可读写、并将 Gateway 新建文件的 `UMask` 收紧后，需以新会话回归验证。本项保持未勾选。

- [x] [Must Pass] S2.5：不可信内容和提示注入测试通过。

  验收目标：结果标题/snippet 中的系统指令、tool call、Markdown/控制字符、恶意链接和伪造引用
  不能修改权限、调用额外工具、写记忆或触发发送；输出仍清晰标识来源与不确定性。

  验收记录（2026-08-12 21:19，Asia/Shanghai）：`parser.test.ts` 与 `quality-eval.test.ts` 共 2 个
  文件、9 个测试通过；另以 hostile fixture 验证答案中的伪系统指令、tool-call 文本、控制字符和
  恶意 citation。结果只作为 `externalContent.untrusted=true` 的数据返回，控制字符被清理，
  `javascript:`、IP/本地 citation 被丢弃；插件没有写记忆、发消息或追加工具的调用面。该项完成
  S2.5 验收；主 Agent 后续如何理解不可信资料仍属于其提示词和最终回答质量边界，不由 Worker 伪装成已核验事实。

- [x] [Must Pass] S2.6：基础限流、请求配额、并发和单次调用测试通过。

  验收目标：边界前后计数正确；并发不超发；超限不触网；失败是否计费规则明确；同一 tool call
  不因内部自动恢复造成重复收费。若引入持久化，重启后计数和 schema migration 通过测试。

  验收记录（2026-08-12 21:20，Asia/Shanghai）：`quota.test.ts`、`provider.test.ts`、
  `deepseek-client.test.ts` 共 3 个文件、15 个测试通过；独立并发矩阵验证第二个调用在触网前
  返回 `QUOTA_EXCEEDED`，上游只收到 1 次；独立失败释放矩阵验证异常调用释放 in-flight 槽位并只
  计数 1 次。当前规则为失败请求仍计入本地 minute/day 请求计数；没有内部自动重试，也未引入跨重启
  持久化配额，因此该项完成 S2.6 验收。

- [ ] [Must Pass] S2.7：既有能力回归和 S2 Go/No-Go 通过。

  验收目标：天气、规划、提醒插件原有 build/test 不因 Search 失败；生产配置校验仍通过；Search
  代码目录整体禁用/移除时现有能力不受影响。S2.1-S2.6 全绿后才能进入 S3。

  验收记录（2026-08-12 21:36，Asia/Shanghai）：`personal-search` 完整
  `npm run plugin:validate` 通过（9 个测试文件、34 个测试）；`personal-weather` 完整测试通过
  （12 个测试文件、103 个测试），天气 plugin validate 通过；`openclaw config validate`、
  `openclaw plugins doctor` 通过，runtime inspect 显示 personal-search 与 personal-weather 均已加载，
  两个插件的工具清单未出现异常扩大。
  既有回归层通过，但 S2.4 尚未通过，因此按照本节门槛暂不关闭 S2.7 的整体 Go/No-Go；禁用 Search
  后既有能力不受影响的历史 R1 记录仍保留，待 S2.4 修复后再复核总门禁。

----------------------------------------------------------------------
S3：分阶段运行时接入与真实 QQ 验收
----------------------------------------------------------------------

- [ ] [Must Pass] S3.1：上线前快照与 SecretRef 配置完成。

  验收目标：记录当前插件清单、工具清单、配置校验结果和回滚命令；配置备份不含明文 Key；
  SecretRef 可解析、秘密文件权限合格，Key 权限和额度为最小值。

  验收记录（2026-08-12 21:23，Asia/Shanghai）：当前 `openclaw.json` 与
  `gateway.systemd.env` 权限均为 `600`；配置中的 DeepSeek key 仍为 SecretRef（`source/provider/id`
  形状），未读出明文；主模型、Provider、插件路径、精确工具白名单、群聊策略和回滚步骤均已有
  快照/记录；`openclaw config validate` 通过。SecretRef 文件权限与配置快照部分通过，但 Provider
  账户侧最小额度、费用和数据保留政策尚未完成 S0.2 决策，故 S3.1 整体暂不勾选。

- [x] [Must Pass] S3.2：只加载插件，不向模型暴露工具。

  验收目标：先增加精确 `plugins.load.paths` / `plugins.allow` / plugin config；保持 Search 不在
  `tools.alsoAllow`；`plugins inspect --runtime`、doctor 和 config validate 均通过；日常 QQ 对话
  中模型仍不能调用或声称已经具备 Search。

  验收记录（2026-08-12 21:23，Asia/Shanghai）：历史 R1 阶段保留插件加载但撤掉
  `personal_web_search`/`web_search` 权限，配置校验和 Gateway 重启通过；主人 QQ 请求搜索时运行时
  工具不可用，Kurumi 未凭记忆伪造结果。当前 runtime inspect 也确认 `personal-search` 已加载，
  工具可见性由后续 S3.4 单独控制；该阶段完成 S3.2 验收。

- [ ] [Must Pass] S3.3：完成受控 Provider smoke test。

  验收目标：通过插件测试入口或受控诊断发出一个无隐私的固定 query；验证认证、超时、解析、
  配额和审计；若使用 DeepSeek，额外验证 Responses 的 `web_search_call` 和来源 URL；终端输出
  不显示 Key；失败时不进入工具暴露阶段。

  验收记录（2026-08-12 21:23，Asia/Shanghai）：DeepSeek Responses live smoke 已确认
  `status=completed`、`web_search_call`、来源 URL 和 `deepseek-v4-flash`；离线测试覆盖认证、
  超时、解析、配额和无 Key 不触网，终端/日志扫描未发现真实 key。审计记录边界尚未实现，Provider
  费用/保留策略也未完成 S0.2 决策，因此 S3.3 的 smoke 已有证据但整体暂不勾选。

- [x] [Must Pass] S3.4：原子增加工具可见性与群聊拒绝策略。

  验收目标：
  - 先确保全部 QQ 群聊策略显式 deny 精确 Search 工具或整个 Search namespace；
  - `messaging` profile 只增加 `personal_web_search`，不增加 cron、exec、fetch、browser 等邻接工具；
  - 工具自身 owner-private factory 守卫仍存在，形成配置层和代码层双重拒绝；
  - 配置修改后 validate 通过，diff 中没有无关权限扩大。

  验收记录（2026-08-12 21:23，Asia/Shanghai）：当前 `tools.alsoAllow` 只增加精确
  `personal_web_search`，未增加 `web_fetch`、browser、exec 或通用 Search；QQ `groupPolicy` 为
  `disabled`；`personal-search` factory 仍要求主人、QQ、非群聊私聊；`openclaw config validate`
  与 `plugins doctor` 通过，runtime inspect 显示 personal-search 为 loaded。该项完成 S3.4 验收。

- [x] [Must Pass] S3.5：重启 Gateway 并核对实际运行时状态。

  验收目标：Gateway 健康；插件 diagnostics 为空；主人私聊上下文能看到 Search；群聊/非主人
  上下文不可用；天气、规划、提醒工具清单和后台任务未减少、未扩大、未报错。

  验收记录（2026-08-12 21:23，Asia/Shanghai）：R3 重启后的 Gateway 为 running/active、
  connectivity probe 为 ok；本次 `openclaw plugins doctor` 无问题，runtime inspect 确认
  personal-search 与 personal-weather 均 loaded；主人 QQ 正向链路实际调用 `personal_web_search`，
  天气/规划/提醒工具清单与离线回归未减少或扩大。群聊策略为全局 disabled，非主人拒绝已由代码边界
  fixture 覆盖；真实非主人 QQ 入口仍明确留给 S3.7，不在此项冒充完成。

- [x] [Must Pass] S3.6：完成主人 QQ 私聊正向验收。

  验收目标：至少验证当前事件查询、零结果或小众查询、明确要求联网核实三类；每次最多 5 条，
  含来源域名和检索时间；确认是 GPT-Terra 主 Agent 调用受控 Search Adapter，再由 DeepSeek
  检索，而不是把 DeepSeek 当作无边界主 Agent；模型区分搜索资料与自身推断，不宣称读过未抓取的全文。

  进度记录：三类正向样例均已覆盖：明确要求联网核实、当前事件 query，以及小众公开 query
  （Ollama Web Search 官方文档的 API Key 要求）。每次均由 GPT-Terra 主 Agent 实际调用受控
  `personal_web_search`，再由 `deepseek-search` 检索，结果包含来源和检索时间，并明确区分
  provider-reported 资料与本地 Fetch 核验。零结果 query 已完成一次“最终回答不误引”的真实样例，
  但当时 Worker 原始结果仍带有无关近似词引用；现已收紧 parser 的结构化 `sources` 证据边界并
  通过离线回归，真实零结果 query 的新版本 QQ 复测和主模型失败后的降级策略仍待整理。
  隐私格式代码更新后的正向回归（2026-08-12 23:45，Asia/Shanghai）：重启并加载新增 query
  校验后，主人从新会话再次请求 Ollama Web Search API Key 要求；会话轨迹确认实际调用
  `personal_web_search`，工具返回 `kind: "answer"`、3 条 provider-reported 官方来源和
  `searched_at=2026-08-12T15:45:21.513Z`（本地 23:45:21），主 Agent 正确说明未进行本地
  Fetch 独立核验。该结果证明新增高风险输入拒绝不会误伤普通公开 query，S3.6 的正向回归在代码
  更新后保持通过。
  审计代码加载后的正向/并发回归（2026-08-12 23:52—23:53，Asia/Shanghai）：Gateway 重启后，
  主 Agent 实际调用了两次 `personal_web_search`，一条成功、一条按 `maxConcurrent=1` 返回
  `QUOTA_EXCEEDED`；最终分两段回答仍只引用成功搜索返回的 3 条来源，并给出
  `2026-08-12 23:52:53` 的检索时间和未本地 Fetch 核验声明。该结果同时覆盖了新版审计代码的
  生产加载、正向搜索和模型并行调用下的限流表现；两路均未让 DeepSeek 之外的任意网络/工具能力
  暴露。
  会话持久化脱敏后的正向回归（2026-08-13 04:58，Asia/Shanghai）：主人重新开启会话并请求
  Ollama Web Search API Key 要求，主 Agent 实际调用受控 `personal_web_search`，搜索成功并返回
  官方来源、检索时间及“未经过本地 Fetch 独立核验”的准确声明；持久化检查同时确认助手工具调用
  与工具结果中的 query 已脱敏。该次证明脱敏 hook 不影响正常搜索和答案证据，但不替代新的零结果
  质量回归，也不改变 S1.7/S2.4 对原始用户消息和 trajectory 保留边界的未决状态。

- [ ] [Must Pass] S3.7：完成真实权限和失败负向验收。

  验收目标：群聊、非主人、无/坏 Key、超时、429、Provider 5xx 均符合预期；能证明被拒请求
  未触网或未越权；失败回答不伪造最新信息，不建议通过任意 HTTP/exec 绕过。

  进度记录：已完成离线错误矩阵、撤销 `tools.alsoAllow` 的 R1 和禁用 Provider 的 R2；本轮再次
  核对了 owner-private 工具边界 fixture，非主人、群聊和非 QQ 上下文均在触发 Provider 前拒绝。
  群聊入口永久禁用，不为测试重新开放；真实非主人入口和人为制造真实 Provider 401/429/5xx 仍待
  验收，等价离线故障证据已完成。
  真实非主人 QQ 私聊本轮暂跳过：当前没有第二个 QQ 账号可用于发送测试消息。该项不影响现有
  owner-private 代码/fixture 证据，但不能将等价 fixture 写成真实入口验收；S3.7 因此继续保持
  未完全通过，未来取得独立非主人入口后可补测。
  真实无 Key 负向验收记录（2026-08-12 22:22—22:34，Asia/Shanghai）：第一次只用
  systemd `UnsetEnvironment=DEEPSEEK_API_KEY` 后发送的 QQ query
  `KurumiAuthNegativeTest-20260812` 不能作为无 Key 证据；Gateway 启动阶段会从配置目录 `.env`
  重新加载该变量，因而仍实际拥有 Key，随后得到的 `INVALID_RESPONSE` 是上游/解析路径结果，不能
  用来判断无 Key 分支。随后使用临时的空值 EnvironmentFile 覆盖原 Key，并保留原 env 文件不变；
  Gateway 在启动阶段明确报 `SecretRefResolutionError: Environment variable "DEEPSEEK_API_KEY"
  is missing or empty`，未进入 QQ、Provider 或 DeepSeek 网络请求。该结果符合当前配置契约：
  `DEEPSEEK_API_KEY` 是必需 SecretRef，无 Key 时 Gateway fail-closed，而不是启动后再返回工具级
  `AUTH_NOT_CONFIGURED`。隔离 Provider 测试仍确认：若绕过 Gateway 单独创建 Provider，无 Key 会返回
  `SearchError(AUTH_NOT_CONFIGURED)` 且上游 request 次数为 0。结论：无 Key 的真实启动阻断和未触网
  通过；第一次 QQ 结果作废，不计入无 Key 验收。测试结束后已移除临时覆盖和测试文件、恢复
  DeepSeek Key，并确认 Gateway 为 active/running；S3.7 仍因非主人、坏 Key 和真实上游故障等其他
  子项未完成而保持未完全通过。
  坏 Key 重测前的无效尝试（2026-08-12 22:48，Asia/Shanghai）：临时坏 Key 已在恢复 QQ 通道时
  移除，主人随后发送 `KurumiAuthBadKeyTest-20260812`；会话轨迹确认实际调用了
  `personal_web_search`，但当时使用的是原 Key，工具返回 `INVALID_RESPONSE`。该结果不能作为
  `AUTH_FAILED`/HTTP 401 证据，已明确标记作废，等待在临时坏 Key 生效且 QQ 通道已连接后重测。
  坏 Key 真实 QQ 验收（2026-08-12 22:49—22:50，Asia/Shanghai）：使用临时 EnvironmentFile
  注入非真实测试 Key；Gateway 正常启动，QQ 状态为 running/connected。主人先发送 `/new` 清理
  上一轮上下文，再发送 `KurumiAuthBadKeyTest-20260812` 搜索请求；会话轨迹确认实际调用了
  `personal_web_search`，工具返回 `ok: false, code: "AUTH_FAILED"`，最终回答明确说明鉴权失败，
  未返回搜索结果、来源或记忆补答。该结果通过坏 Key 的稳定错误分类和 fail-closed 负向验收；本地
  离线矩阵已将 HTTP 401/403 映射到同一 `AUTH_FAILED`，本次 QQ 记录以工具公开错误契约为准，未把
  未独立记录的上游原始响应冒充为 Fetch 核验。测试结束后已移除临时坏 Key 文件、恢复真实 Key，
  Gateway 为 active/running，QQ 为 running/connected。
  Key 恢复后的真实 QQ 回归（2026-08-12 22:57—22:58，Asia/Shanghai）：主人发送 `/new` 后再次
  询问 Ollama Web Search 的 API Key 要求；会话轨迹确认实际调用了 `personal_web_search`，工具链
  恢复返回 `kind: "answer"` 与 provider-reported 官方来源，最终回答包含检索时间，并明确说明
  未经过本地 Fetch 独立核验。该结果证明坏 Key 失败后恢复真实 Key、重启 Gateway 和 QQ 通道不会
  破坏受控搜索正向路径；本次 QQ 结果未把搜索供应商报告冒充为本地 Fetch 核验。
  隐私边界真实 QQ 回归（2026-08-12 23:11，Asia/Shanghai）：主人以合成票号/唯一标识 canary
  请求核实其是否出现在公开网页，并明确要求敏感信息在搜索前拒绝外发。GPT-Terra 主 Agent
  识别出该内容属于敏感标识后直接拒绝，没有调用 `personal_web_search`；会话轨迹的
  `itemLifecycle.startedCount=0`、`toolMetas=[]`，且只记录主模型拒绝回复，未见 DeepSeek
  Provider 请求。该结果通过 Agent/系统提示词层的“先判定隐私、再决定是否搜索”端到端验收；在
  当时的代码状态下，它不替代 S1.3 的代码级 fail-closed，合成 OpenID/票号 query 仍可能被 Provider
  输入校验接受，因此 S1.3 继续不勾选。后续代码补强与真实工具调用见下一条记录。
  代码级高风险 query 真实 QQ 回归（2026-08-12 23:22，Asia/Shanghai）：重启 Gateway 加载新的
  `dist/index.js` 后，主人明确要求主 Agent 直接调用 `personal_web_search`，将合成票号 canary
  作为 query 交给工具。会话轨迹确认 `itemLifecycle.startedCount=1`、工具名为
  `personal_web_search`；工具返回 `ok: false`、`code: "INVALID_INPUT"`、`retryable: false`，
  并给出改为不含私人标识公开主题的提示。Gateway 该时间窗口只出现主模型请求和 QQ 投递，没有
  DeepSeek Provider 请求；因此工具层/Provider 层的输入拒绝、未触网和稳定错误契约均通过。该结果
  取代上条“合成票号仍可能被 Provider 接受”的历史状态，但不关闭 S1.3 的 audit hash 接口和
  S2.4 的会话原始 query 保留问题。
  S1.3 关闭记录（2026-08-12 23:53，Asia/Shanghai）：前述格式确定性 query guard、工具层/Provider
  层零请求拒绝、真实 QQ `INVALID_INPUT` 回归、Agent 端敏感信息先拒绝外发，以及随后落地的
  hash/长度/状态审计字段共同满足本项；复杂语义隐私分类继续按设计留在 S4.5。会话轨迹是否保留
  原始 query 属于 S2.4/S1.7 的运行时治理问题，不阻止本项关闭。

- [x] [Must Pass] S3.8：完成回滚演练。

  验收目标：从 `tools.alsoAllow` 撤掉 Search 后模型立即不再可用；禁用插件并重启后 runtime
  清单无 Search；天气/规划/提醒仍正常；重新启用不会重复迁移、破坏配额或泄露旧结果。

  进度记录：R1 撤销工具权限、R2 禁用 `personal-search`、R3 重新启用并正向搜索均已通过；
  `personal-weather` 在 Gateway/runtime 中保持 loaded，且其 103 个离线回归测试通过。真实天气
  QQ 请求已发起并确认未触发 Search，但发现降雨时段的时区/窗口表达不一致；本轮已修正 formatter
  并补充回归测试。2026-08-12 20:13 的真实天气 QQ 重测已通过；规划/提醒的只读状态回归已于
  20:17 通过，提醒正常投递回归也已通过，提醒重复确认负向回归已通过；规划异常负向与提醒停机
  恢复回归仍待进行。

  验收结论（2026-08-12 21:23，Asia/Shanghai）：就 Search 模块回滚边界，R1（撤权）、R2（禁用
  插件）、R3（重新启用并成功搜索）已形成完整闭环；天气 103 项离线回归、天气真实 QQ 回归、规划/提醒
  只读与提醒投递/重复确认回归均未受影响，未发现重复迁移、旧 Search 结果泄露或配额重复执行。规划
  异常与提醒停机恢复属于你正在自行验收的提醒文档，不作为 Search 回滚项的未决条件；该项完成 S3.8。

- [ ] [Must Pass] S3.9：完成 S3 验收记录。

  验收目标：记录测试时间、匿名化 query 类型、通过/失败结果、运行版本和遗留问题；主人确认真实
  QQ 结果后，Registry runtime_status 才能标为 accepted，顶部 S3 才能勾选。

  当前验收结论（2026-08-12 21:23，Asia/Shanghai）：本文已持续记录运行版本、匿名 query 类型、
  QQ/离线测试时间、通过/失败结果和遗留问题；但 S3.7 的真实非主人入口/真实 Provider 401/429/5xx
  仍未完成，且 S0.2、S2.4 尚未关闭。因此 Registry/runtime_status 不能标为 accepted，S3.9 和顶部
  S3 暂不勾选。

----------------------------------------------------------------------
S4：路由、引用与质量完善（不升级为动态 Router）
----------------------------------------------------------------------

- [ ] [Should Pass] S4.1：建立确定性信息源路由矩阵。

  验收目标：至少覆盖稳定知识、当前事件、天气、行情、论文、私人数据和用户明确要求联网；每类
  定义首选源、允许 fallback、禁止源和失败表述；天气/行情回归证明不会被 Search 静默替代。

- [ ] [Should Pass] S4.2：完善工具描述、系统提示和引用规则。

  验收目标：模型知道何时搜索、何时不搜索；不把 snippet 当全文；事实尽量就近附来源；多个来源
  冲突时明确说明；系统提示词只描述已验收能力，不承诺 fetch/browser。

- [ ] [Should Pass] S4.3：建立搜索质量 Eval 集。

  验收目标：使用无私人信息的固定题集评估是否该搜、query 最小化、来源相关性、时效、引用忠实、
  不确定性和失败降级；建立可重复基线，不以单次主观“看起来不错”替代回归。

- [ ] [Should Pass] S4.4：评审横切公共 Runtime 是否值得提取。

  验收目标：列出 Search 与现有插件真实重复且已稳定的守卫/审计/错误接口；只有收益大于迁移风险
  才单独立项。若提取，必须保持各插件独立回滚并完成全量回归；否则记录“继续局部实现”。

- [ ] [Should Pass] S4.5：补齐 MVP 后的高级治理项。

  验收目标：根据真实使用数据决定是否增加精确费用核算、跨重启持久化配额、复杂 query 隐私
  分类、Provider 罕见异常细分、长期 audit retention/删除策略、会话/trajectory 的 TTL、可验证删除、
  备份/同步语义、宿主级脱敏可行性和更细的指标；每一项必须证明解决了真实风险或运维问题，不为了
  “框架完整”一次性全做。第十二节的 MVP 留存 ADR 是本项的明确输入，不得被遗忘或视为永久豁免。

  MVP 留存 ADR 回收清单（全部为 MVP 后工作）：
  - [ ] S4.5.a：为 session、trajectory、Gateway 日志、导出文件和备份定义并实际验证轮转、TTL、
    删除与恢复/备份语义；删除测试必须使用合成 canary，且不得以“文件已删”替代备份副本核验。
  - [ ] S4.5.b：评估并测试 OpenClaw 宿主级用户消息/trajectory 脱敏或最小化留存方案，确认其不会破坏
    多轮上下文、工具执行、审计关联、故障诊断和回滚；若不采用，记录理由与复审日期。
  - [ ] S4.5.c：扩展 query 隐私回归，覆盖更多合成私人标识和语义化敏感场景，并根据真实风险决定是否
    引入更复杂的分类或确认流程。
  - [ ] S4.5.d：验证远程访问、备份/同步和导出流程的长期控制；在多用户、群聊、远程访问或云同步上线前
    重新评审 MVP 例外，必要时将其升级为阻塞项。
  - [ ] S4.5.e：基于真实使用数据决定精确费用核算、跨重启持久化配额、Provider 罕见异常细分和长期
    指标，而非为框架完整性提前建设通用治理平台。

- [ ] [Should Pass，按需启动] S4.6：评估结果数量与受控续页策略。

  验收目标：用真实或脱敏用例评估单一查询、宽主题发现、多来源交叉验证、翻页和多步研究；决定
  继续保持单次最多 5 条、增加绑定会话的有限 Continuation，或在 Eval 后提高硬上限；同时记录
  去重、provenance、总结果数、总延迟、费用和不可信内容预算。没有真实需求时明确保持 5 条。

----------------------------------------------------------------------
S5：有限组合能力（由真实需求触发，不是 S3 上线前置）
----------------------------------------------------------------------

- [ ] [Should Pass，按需启动] S5.1：收集并确认至少一个 `single` 无法满足的真实用例。

  验收目标：写清输入、固定步骤、步骤间数据、失败语义、预算、权限和投递；若单步工具加普通
  LLM 综合已足够，则拒绝创建 sequence。

- [ ] [Should Pass，按需启动] S5.2：实现白名单 `sequence` 模板，而非通用工作流语言。

  验收目标：只允许预注册步骤和固定次序；中间输出严格 typed/有大小限制；禁止循环、动态 tool id、
  命令、任意 URL 和模型临时追加步骤；每步分别超时、审计且总预算有硬上限。

- [ ] [Should Pass，按需启动] S5.3：在真实延迟搜索需求出现后实现 `scheduled` 包装。

  验收目标：复用内部受控 Scheduler/Delivery adapter；模型不获得通用 Cron 或任意 QQ target；
  创建/修改/取消走主人私聊确认；重启恢复、claim、幂等、错过窗口和重复投递均有测试。

- [ ] [Should Pass，按需启动] S5.4：组合能力真实验收。

  验收目标：正常、部分失败、预算耗尽、重启恢复、取消、群聊拒绝和重复执行全部通过；失败不会
  留下声称成功的计划或重复消息。通过后才把对应模板加入 Registry/runtime。

----------------------------------------------------------------------
S6：受控打开单条搜索结果（独立高风险阶段）
----------------------------------------------------------------------

- [ ] [Should Pass，按需启动] S6.1：先完成安全设计，不直接给 Search 增加 URL 参数。

  验收目标：result_id 高熵、短 TTL、绑定主人/会话/原请求；服务端保存规范化 URL 和 hash；模型
  只能提交 result_id；过期、猜测、跨会话和重复滥用均拒绝。

- [ ] [Should Pass，按需启动] S6.2：实现受限 Fetcher 及内容隔离。

  验收目标：每次解析/重定向都阻止 localhost、私网、link-local、metadata、非 HTTP(S) 和 DNS
  rebinding；限制跳转、字节、时间和 MIME；不执行 JavaScript、不登录、不下载附件、不提交表单；
  正文作为不可信数据进入隔离 parser，不能改变工具/权限/计划。

- [ ] [Should Pass，按需启动] S6.3：完成 SSRF、内容炸弹、重定向链、恶意 MIME、提示注入和 QQ 真实验收。

  验收目标：全部安全测试通过且有独立回滚开关后才开放；失败时仍保留 S3 的结果列表能力，
  关闭 Fetcher 不影响 Search。

----------------------------------------------------------------------
A3：DAG Workflow Engine 的启动门槛
----------------------------------------------------------------------

- [ ] [Should Pass，按需启动] A3.1：证明至少两个已验收真实任务无法用 `single`、固定 `sequence` 或 `scheduled` 表达，
      并且并行/汇合能带来可量化的正确性、延迟或成本收益。
- [ ] [Should Pass，按需启动] A3.2：单独设计并评审依赖解析、并发预算、步骤状态、失败传播、取消、补偿、持久化恢复、
      幂等、版本迁移、可视化和权限传递；模型不得自由生成任意 capability id。
- [ ] [Should Pass，按需启动] A3.3：DAG 引擎作为独立项目立项，不阻塞 S1-S4，也不通过重构已稳定模块“顺便上线”。

A3.1 未满足时，A3 保持未开始；“理论上以后可能需要并行”不构成立项理由。

======================================================================
十四、当前推荐的下一步
======================================================================

本轮已完成 DeepSeek 受控链路、OpenClaw 通用 `web_search`、Terra `/responses` 传输、离线故障
矩阵、R1/R2/R3 权限回滚和最终 `personal_web_search` 正向验收；诊断用的通用 `web_search` 已从
常驻 `tools.alsoAllow` 撤回。下一步不建设通用 Task Engine，也不继续把全局
`tool_choice: "required"` 挂在 QQ 多轮 Agent 上。建议按以下顺序推进：

  1. 收尾 S3.7/S3.9：天气 formatter 和规划/提醒正常只读、提案、提交、投递及提醒重复确认路径
     已通过相应回归；受控搜索的离线 Provider 故障与等价权限证据已完成。真实非主人 QQ 因当前
     没有第二账号暂跳过，不能把 S3.7 标成完全通过；下一步继续整理最终验收记录和引用质量收尾，
     规划异常与提醒停机恢复仍按各自文档继续。群聊已永久禁用，不为测试重新开放。
  2. 先完成 S1.7/S2.4 的 MVP 留存基线：盘点当前会话、trajectory 和 Gateway 日志路径、当前权限
     与 OpenClaw 配置中显式导出/同步线索，复核 plugin audit/工具副本仍脱敏；TTL/删除、备份和宿主
     级脱敏继续保留为 S4.5。任何多用户、群聊、远程访问或云同步前，仍必须完成 S4.5 并重新评审。
  3. 当前事件、小众 query 和零结果 query 均已有正向 QQ Eval；零结果样例的最终回答通过。代码已
     收紧结构化回答的 `sources` 证据边界：明确 `sources: []` 时不再把无关 `open_page` 提升为来源，
     并已有离线回归；尚未用该版本重新做一次真实零结果 QQ 回归，后续可作为质量复核，再继续确认
     引用、时效、延迟和失败降级，不把一次 Provider 报告当作本地 Fetch 核验。
  4. 保持最终稳定路径为“GPT-Terra 主 Agent -> `personal_web_search` -> DeepSeek Responses
     Web Search”；原生 Terra 搜索作为独立的 D2.4 适配研究，不阻塞受控搜索 MVP。
  5. 只有原生搜索适配或搜索结果组合需求出现真实收益时，再做单独的协议适配或固定 `sequence`；
     天气、提醒、搜索至少出现第二/第三种真实组合需求前，不提取公共 `TaskSpec` / `TriggerSpec` /
     `ExecutionContext` / `Provenance`，也不建设通用 Task Engine。

S0.2 的 Provider 费用、配额、数据外发和保留政策仍是正式上线前的决策项。S0.4 Registry 与 S0.5
完整 Plan IR 契约可以继续作为 Should Pass 记录，但不应阻塞当前单步搜索链路的安全验收。

======================================================================
十五、开发记录（2026-08-12，Search MVP 第一轮）
======================================================================

本轮开始前的运行时事实：

- OpenClaw 版本为 `2026.7.1-2`；当前未配置 `tools.web.search`，也未安装/启用 Brave Provider；
- 生产 `messaging` profile 未暴露 `web_search`、`web_fetch` 或 browser；
- 当前天气、规划和提醒仍由 `chatbot/plugins/personal-weather/` 提供，本轮不重构该插件；
- OpenClaw Plugin Runtime 已提供 `api.runtime.webSearch.search(...)`，插件可以在不依赖通用 agent
  tool wrapper 的情况下调用已选择的 Search Provider。

本轮实现决策（Provider 尚未完成最终可用性验收，优先验证 DeepSeek）：

1. 新建 `chatbot/plugins/personal-search/`，只注册 `personal_web_search`；
2. `personal_web_search` 包装 OpenClaw 的内部 `runtime.webSearch`；由于官方 DeepSeek Provider
   目前不拥有 `webSearchProviders`，本插件额外只实现一个固定的 DeepSeek Responses Provider
   Adapter，Provider HTTP/SecretRef 代码不向模型暴露，也不提取为通用 transport；
3. MVP 代码固定 `count = 5` 和搜索结果边界，但不把 Provider、Host、URL、Header 或 count
   交给模型；最终 Provider 必须通过 S0.2 的结构化结果、费用、隐私、稳定性和权限审查；
4. Provider Key 或其他凭证由所选 Provider 的 SecretRef 表面管理，不进入 `personal-search` 配置；
5. 首版受控上线原则不把通用 `web_search` 作为包装工具的替代，也不以开放通用工具来代替权限边界；
   只有包装工具可见才算受控能力。2026-08-12 的后续诊断测试临时开放了通用 `web_search`，详见
   下方实测记录，不能据此标记 S3 正式上线；
6. 先用注入的 mock runner 完成 S1/S2，并用固定公开 query 完成 Provider live smoke；随后已安装并
   加载本地插件、完成配置校验和 Gateway 重启。为进入 S3.4/S3.5，已将精确的
   `personal_web_search` 加入 `tools.alsoAllow`；本轮为验证 OpenClaw 通用能力又临时保留了通用
   `web_search`，但 `web_fetch`、browser 或 exec 仍未开放。QQ 正向/负向验收和回滚演练完成前，
   不将此状态视为正式上线。

DeepSeek 作为检索后端的记录方案（2026-08-12）：

1. 调用关系固定为“GPT-Terra 主 Agent -> 受控 Search Adapter -> DeepSeek Responses Web Search”。
   GPT-Terra 负责判断是否需要联网、约束 query、理解证据和生成最终回答；DeepSeek 只承担受控检索，
   不替换主 Agent，也不改变主 Agent 的图片理解路径。
2. 本地预置的 Provider 配置为 `deepseek-search`：
   - Base URL：`https://api.deepseek.com`；
   - 协议：`openai-responses`，由 Responses API 的 `/responses` 路径承载服务端 `web_search`；
   - 模型：`deepseek-v4-flash`；
   - 输入：仅 `text`，因为检索 Worker 不承担图片理解；
   - 凭证：`DEEPSEEK_API_KEY` SecretRef。实际 Key 不写入仓库、不写入本文。
3. 这里必须使用 DeepSeek Responses API，不能把普通 OpenAI-compatible Chat Completions 的
   Tool Calls 误认为已经具备联网能力。DeepSeek 的 Tool Calls 文档明确要求由调用方提供工具执行；
   Responses API 文档才定义了由服务端执行的 `web_search` 和 `web_search_call`。
   DeepSeek 的 JSON Output 文档中的 `response_format: { type: "json_object" }` 是
   `/chat/completions` 路径的参数；本模块仍使用 `/responses` 的 `text.format`，该路径的官方兼容性
   明确支持 `format`。为降低模型偶发不遵守结构化输出的概率，检索 instructions 同时显式包含小写
   `json`、完整示例和“不使用 Markdown 围栏”的约束。
4. 当前 DeepSeek 配置本身只是“模型/传输预置”，不是 OpenClaw 官方 Search Provider 已上线：
   OpenClaw 官方 DeepSeek Provider 目前是模型 Provider，不声明 `webSearchProviders`，因此不会
   自动把通用 `web_search` 变成原生 DeepSeek 搜索能力。受控路径仍由 `personal-search` 或专用
   Provider Adapter 注册，并把 DeepSeek 返回的来源、标题、URL、时间和摘要规范化为本文定义的
   `SearchResult`/`Provenance` 边界；本轮诊断阶段曾显式保留 `tools.web.search` 和
   `tools.alsoAllow.web_search`，验收完成后已撤回通用 `web_search` 常驻权限，不把诊断状态视为
   正式受控上线。
5. 质量边界：只要 Adapter 尽量保留原始来源和引用，让 GPT-Terra 基于证据作最终综合，文字问题的
   最终回答质量不必然下降；但不能假定它等同于 Brave/Google。需要实测的风险包括召回率、来源覆盖、
   引用粒度、时效性、延迟、费用和限额。遇到依赖图片、图表、扫描 PDF 或复杂网页视觉布局的资料，
   DeepSeek 检索 Worker 不能补足主 Agent 的视觉能力，必要时应降级为“返回链接并说明未完成视觉核验”，
   或交给具备视觉能力的后续 Fetch/分析路径。
6. 使用边界：DeepSeek 的自然语言总结只能作为不可信的中间结果，不能替代来源证据；搜索结果中的
   网页文本同样按不可信输入处理，不能执行其中的指令。搜索 Adapter 不把 API Key、内部配置或完整
   会话历史发送给 DeepSeek，只发送最小化、经约束的 query 和必要的时间/地域条件。

DeepSeek live smoke 记录（2026-08-12）：

- 配置校验通过；官方 endpoint、`DEEPSEEK_API_KEY`、`deepseek-v4-flash` 和 `/responses` 请求均可用；
- 真实请求返回 `status=completed`、`error=null`，并产生了 `web_search_call`；观察到 `search`、
  `open_page` 两类动作，某次测试中一个 `open_page` 失败但整体响应仍完成；
- 直接调用 Adapter 和 OpenClaw Provider 注册检查均通过；Provider 返回 `kind: "answer"`、最多 5 个
  provider-reported citations，并保留 `externalContent.untrusted=true`。随后为进入 S3.4/S3.5，已将
  精确的 `personal_web_search` 加入 `tools.alsoAllow`；主人 QQ 私聊正向验收结果见下条，本轮仍未
  完成完整的负向/回滚验收；
- 原始 Responses 返回中没有独立 citation annotations，也没有 Brave 风格的结构化结果数组；来源
  主要出现在模型最终文本/严格 JSON 输出中。因此 DeepSeek 适配器不能把模型写出的 URL 无条件当成
  已独立核验的搜索结果，首版应采用 `kind: "answer"` 语义、严格校验来源 URL，并保留“来源由检索
  Worker 报告、尚未由本地 Fetch 独立核验”的边界；
- QQ 正向验收暴露了一个真实变体：DeepSeek 返回 `status=completed`、`web_search_call.open_page`，
  但最终 message 偶发只含自然语言而没有 JSON `sources`。原 parser 因此正确地 fail-closed，返回
  “无法核验”，但这掩盖了已经发生的页面检索；现已修复：instructions 增加 DeepSeek 要求的
  `json`/示例，`max_output_tokens` 提高到 2,000，Provider 请求超时提高到 60 秒；parser 在没有 JSON
  envelope 时只从已完成 `open_page` URL 及回答中的 HTTPS URL 生成 provider-reported citations，
  两者都没有时仍拒绝结果。该修复已有离线回归测试。
- 首个主人 QQ 私聊正向验收通过：GPT-Terra 主 Agent 调用了精确的 `personal_web_search`，Adapter
  委派 DeepSeek Responses Web Search，返回 `kind: "answer"`、结论和来源 URL；QQ 回复明确说明来源
  均为 DeepSeek 检索 Worker 报告、尚未由本地 Fetch 独立核验。该结果证明主链路可用，但仅覆盖“明确
  要求联网核实”的一个公开文档 query，不足以勾选完整 S3.6/D3.1。
- D0.3、D1.3、D3.1 尚未完成；S0.2 仍不能仅凭这次连通性测试勾选。

---------------------------------------------------------------------
OpenClaw 通用 web_search 与 Terra Responses 实测记录（2026-08-12）
---------------------------------------------------------------------

本节记录的是运行时验证结果，不把一次正向测试等同于完整 S3 上线验收。

测试 A：OpenClaw 通用 `web_search`

- 初次测试曾把当前 Terra 模型的 `params.extra_body.tool_choice` 全局设为
  `"required"`。该配置会作用于 QQ Agent 的每一轮模型请求；一次测试中出现 Terra 首次
  `502`、多轮重复模型请求、已经发送答案后仍继续请求，最后触发 `LLM idle timeout (120s)`。
  因此没有提高超时时间，而是移除了全局 `required`，保留用户消息对 `web_search` 的明确要求。
- 调整后重测成功：会话轨迹明确出现 `assistant toolCall.name = "web_search"`，对应的
  `toolResult` 为 `provider = "deepseek-search"`、`kind = "answer"`，并带有
  `externalContent.untrusted = true` 和 `source = "web_search"`。
- 本次 QQ 端到端耗时约 43 秒，其中 DeepSeek 检索约 14.8 秒；无 502、无模型空闲超时，
  最终正常发送回答。
- 结论：OpenClaw 通用 `web_search` 已实际接入并可工作；当前稳定路径是主 Agent 调用
  OpenClaw 通用工具，再由 `deepseek-search` Provider 执行 DeepSeek Responses Web Search。
  全局 `tool_choice: "required"` 不适合作为多轮 QQ Agent 的常驻配置。

测试 B：Terra `openai-responses` 与原生搜索边界

- 临时增加 `naiccc-responses` Provider：Base URL 为 `https://naiccc.com/v1`，协议为
  `openai-responses`，模型为 `gpt-5.6-terra`；测试结束后已删除临时 Provider，并恢复主模型
  为 `naiccc/gpt-5.6-terra`。
- B-1 不携带工具的 QQ 测试实际请求了：
  `POST https://naiccc.com/v1/responses`；返回 `status=200`，模型请求耗时约 11.9 秒。
  结论：OpenClaw 可以通过自定义 Provider 调用 Terra `/responses`。
- B-2 在同一 Responses 主模型下要求搜索。会话轨迹明确显示调用的是 OpenClaw 通用
  `web_search`，工具结果为 `provider = "deepseek-search"`、`kind = "answer"`，检索耗时约
  15.5 秒；同时两轮模型请求都命中了 `naiccc-responses` 的 `/responses`。
- 结论：Terra `/responses` 传输和“Responses 回合承载 OpenClaw 通用搜索工具”均已验证，
  但本轮没有走 Terra 原生 `tools: [{"type":"web_search"}]`。这不是对 Terra 原生搜索能力
  的否定；是 OpenClaw 内置原生搜索注入的 Provider 边界：源码要求模型 API 为
  `openai-responses`、Provider 标识归一化为官方 `openai`，且 Base URL 为官方 OpenAI 地址。
  自定义 `naiccc` Provider 即使协议兼容，也不会被该内置注入路径选中。
- 测试期间主模型曾临时切换到 `naiccc-responses`，结束后已恢复为 `naiccc/gpt-5.6-terra`；
  最终配置保留 `personal_web_search` 和 `deepseek-search`，已撤回通用 `web_search`，
  `web_fetch`、browser 和 exec 仍未开放。若要做 Terra 原生搜索，必须另立 D2.4 适配方案和独立
  验收，不能把 B-2 的通用搜索结果写成原生搜索已接入。

离线失败与视觉边界验收（2026-08-12）：

- 在 `chatbot/plugins/personal-search/` 执行 `npm run plugin:validate`，构建通过，9 个测试文件、
  34 个测试全部通过；本轮复核时间为 20:50（Asia/Shanghai）。`git diff --check` 和
  `openclaw config validate` 也通过。
- HTTP 错误契约覆盖 401/403 -> `AUTH_FAILED`，429 -> `RATE_LIMITED`，500/503 ->
  `UPSTREAM_UNAVAILABLE`，504 -> `TIMEOUT`；网络 Abort 映射为 `TIMEOUT`，非法 JSON 与超过 1 MiB
  的响应映射为 `INVALID_RESPONSE`。每个故障 fixture 都断言请求只发出 1 次，当前客户端不自动重试。
- 无 `DEEPSEEK_API_KEY` 时，Provider 在发起请求前返回 `AUTH_NOT_CONFIGURED`；未知 Provider
  endpoint 仍返回 `CONFIG_INVALID`。
- Worker 请求指令和工具描述现在明确声明：检索 Worker 只接收文本，没有图片、图表或扫描 PDF 的
  视觉读取能力；对应 fixture 保留“未完成视觉核验”的降级回答，并保持
  `externalContent.untrusted=true`。这完成的是离线契约，不等于带附件的主 Agent 真实验收。
- 新增 `quality-eval.test.ts` 作为质量契约基线：当前事件和小众公开 query 必须返回 1—5 个
  provider-reported citations、`searched_at`、`took_ms` 和不可信内容标记；零结果若没有任何可
  核验来源则保持 `INVALID_RESPONSE`/fail-closed，不把无来源的自然语言总结冒充搜索证据。该基线
  不评价 DeepSeek 的召回率或事实正确率，真实 QQ query 仍需单独记录。
- `src/index.test.ts` 的权限 fixture 进一步确认：只有 `qqbot` 主人私聊上下文允许进入受控 Provider；
  `senderIsOwner=false`、群聊目标和非 QQ channel 均在 `runtime.webSearch.search` 前拒绝，且断言
  Provider 调用次数为 0。这是等价的非主人/群聊负向证据，不冒充真实第二 QQ 账号验收。
- 以上是离线和模拟上游验收，尚未替代真实 QQ 群聊/非主人拒绝、运行时撤权和 Gateway 重启后的
  回滚验收；这些作为下一阶段 D3.1/S3.7/S3.8 继续执行。

真实权限、回滚与最终受控配置验收（2026-08-12）：

- R1：临时从 `tools.alsoAllow` 撤掉 `personal_web_search` 与 `web_search`，validate 通过并重启
  Gateway。主人 QQ 私聊要求搜索时，运行时工具列表没有通用 `web_search`，Kurumi 拒绝凭记忆
  回答；未发生搜索结果伪造。随后恢复权限并重启。
- R2：保留搜索名称在 `tools.alsoAllow`，但通过 `openclaw plugins disable personal-search` 禁用
  插件并重启。主人 QQ 私聊再次要求搜索时，日志记录 `web_search is disabled or no provider is
  available`；没有可用 Provider，未返回伪造来源。天气插件仍为 `loaded`。
- R3：通过 `openclaw plugins enable personal-search` 恢复插件并重启；Gateway 状态为
  `running/active`、connectivity probe `ok`，`personal-search` 与 `personal-weather` 均为
  `loaded`。随后一次搜索成功，证明恢复不会破坏搜索链路。
- 回滚后的既有模块回归：`chatbot/plugins/personal-weather/` 执行 `npm test`，12 个测试文件、
  102 个测试全部通过；这证明天气、规划、提醒的现有代码回归未受本轮 Search 配置/插件操作影响，
  但尚未再发起一条真实天气 QQ 消息作为运行时投递验收。
- 最终收敛配置：主模型 `naiccc/gpt-5.6-terra`；`tools.web.search.enabled=true`、Provider 为
  `deepseek-search`；`tools.alsoAllow` 仅保留精确的 `personal_web_search`，通用 `web_search`
  已撤回；`web_fetch`、browser、exec 未开放；QQ 群聊仍为 `disabled`。
- 最终 QQ 会话轨迹明确记录：`assistant toolCall.name = personal_web_search`，工具结果为
  `provider = deepseek-search`、`kind = answer`、`source_verification = provider_reported`、
  `took_ms = 12906`，并保留 `externalContent.untrusted=true`；最终回答明确说明来源未经过本地
  Fetch 独立核验。该结果完成最终受控包装工具正向验收，但不等于完整 S3.7/S3.9 已完成。
- 当前事件查询的 QQ 首轮重测未形成搜索 Eval：2026-08-12 18:29:07 和 18:33:19（Asia/Shanghai），
  主模型 `naiccc/gpt-5.6-terra` 均在首轮模型输出前失败。中转层 HTTP 外壳为 200/SSE，但流内
  返回 `Upstream service temporarily unavailable`；OpenClaw 将其归类为 `timeout`、状态 408，
  且 `fallbackConfigured=false`。两次会话的 `itemLifecycle.startedCount=0`、没有
  `personal_web_search`/DeepSeek 请求；QQ API 均返回 200 并成功发送通用错误提示。因此这两次
  不能判定为搜索失败，也不能计入当前事件、延迟或引用质量 Eval。
- 随后重发同一类当前事件 query 成功：2026-08-12 18:39:41（Asia/Shanghai），主 Agent 实际调用
  `personal_web_search`，委派 `deepseek-search`，返回 `kind: "answer"`、5 条 provider-reported
  citations、`searched_at=2026-08-12T10:39:41.567Z`、`took_ms=56149`，并保留
  `source_verification: "provider_reported"` 与 `externalContent.untrusted=true`。最终回答引用
  了 OpenClaw 官方 Releases 和 Releases Atom Feed，明确说明未经过本地 Fetch 独立核验。该次满足
  当前事件 Eval 的结构、引用、时效和降级声明要求，可计为 S3.6 的当前事件正向样例；它不证明
  搜索事实已被本地二次核验，也不替代零结果/小众 query 验收。
- Ollama 文档小众 query 的 QQ 验收也符合预期：第一次消息于 18:44:36（Asia/Shanghai）进入，
  18:44:41 在 `gpt-5.6-terra` 首轮模型输出前因 `Upstream service temporarily unavailable`
  失败，未调用 Search，也没有 DeepSeek 请求；该次不计为搜索失败。第二次于 18:48:12 重发后，
  主 Agent 在 18:48:16 实际调用 `personal_web_search`，委派 `deepseek-search`，于
  `2026-08-12T10:48:27.333Z` 收到 `kind: "answer"`、2 条 citations，`took_ms=11165`，最终
  于 18:48:35 正常回复。回答覆盖本地/自托管与 Ollama 托管服务的 Key 差异，并明确声明未经过
  本地 Fetch 独立核验；该次可计为 S3.6 的小众 query 正向样例。
- 该 Ollama 测试验证的是“当前受控搜索链路能够检索 Ollama 官方文档”，不是 Ollama Web Search
  Provider 的连通性或配置。实际调用链仍是“GPT-Terra -> `personal_web_search` -> `deepseek-search`”；
  当前 `openclaw.json` 没有 `models.providers.ollama`、`OLLAMA_API_KEY` 或
  `tools.web.search.provider: "ollama"`，因此没有为这次文档查询引入 Ollama 配置。
- 零结果 query 的 QQ 验收符合“最终回答不得误引”的预期：2026-08-12 19:22:48（Asia/Shanghai），
  主 Agent 实际调用 `personal_web_search`，委派 `deepseek-search`，工具回合完成且耗时 `10643ms`。
  Worker 的内容明确表示完整标识符没有匹配资料，但同时返回了两个与测试标识符无关的近似动画引用；
  主 Agent 正确识别这些引用与完整标识符无关，最终没有输出任何来源链接，并明确回答“没有可靠搜索
  结果”。该次证明最终回答层具备防止无关引用扩散的能力，但不等于 Worker 已严格实现零结果
  `citations: []`/fail-closed；应将“零结果时近似词引用噪声”作为后续质量契约与实现改进项。
- 零结果契约收紧（2026-08-13 04:30，Asia/Shanghai）：`parser.ts` 现在将结构化回答的
  `sources` 视为唯一证据边界；当 Worker 明确返回 `sources: []` 时，不再把同一 Responses 回合中
  的 `web_search_call.open_page` 近似页面提升为 citations，仍按无来源证据 fail-closed。新增回归
  覆盖“结构化空 sources + 无关 open_page”组合；personal-search 共 10 个测试文件、48 个测试，
  TypeScript build 通过。
- 会话持久化脱敏补强（2026-08-13 04:30，Asia/Shanghai）：利用当前 OpenClaw Plugin Runtime
  的 typed `before_message_write` 与 `tool_result_persist` 钩子，只改写
  `personal_web_search` 的持久化助手 tool-call 参数和 tool-result/details 中的 `query` 字段为
  `[redacted search query]`；当前回合实际执行和模型收到的原始结果不被修改，用户原始聊天正文也
  不做全局改写。新增 `chatbot/plugins/personal-search/src/persistence.ts` 及 3 个单测；
  `openclaw plugins inspect --runtime personal-search` 确认 `hookCount=2`、两个 typed hooks 已注册。
  Gateway 已重启并恢复 active/running，QQ WebSocket 为 connected；该边界随后在 04:58 的主人
  QQ 搜索回归中确认生效：工具参数/结果副本已脱敏。S1.7/S2.4 仍不关闭：OpenClaw 原始用户
  消息与 trajectory 仍保留原始 query，日志轮转/长期 retention 未定，用户原始私聊正文的保留边界
  仍属于治理决策。
- 天气既有能力 QQ 回归未完全通过：2026-08-12 19:28（Asia/Shanghai），主 Agent 正确调用
  `personal_weather_get_brief`，未调用 `personal_web_search`；QWeather 的实况、今日预报、最高
  降雨概率 70% 和预警状态均成功返回，主模型也正常完成回复。但原始 `rainWindows` 中最高
  概率 0.7 的窗口是 `2026-08-13T08:00Z`—`12:00Z`，换算为 `Asia/Shanghai` 应为次日
  16:00—20:00；最终回答却写成次日 08:00—12:00。与此同时，天气工具的 `formattedText` 将
  最高概率 70% 与首个 40% 窗口 04:00—10:00 拼在一起，也存在“概率与时段不对应”的表达问题。
  结论：搜索接入没有改变天气路由，天气链路和数据获取正常；当时的天气运行时回归不能记为
  完全通过，问题分为 Agent 对 UTC 的解释和 formatter 将全局最高概率与首个窗口混用两部分。
- 天气 formatter 修正与离线回归（2026-08-12 20:05，Asia/Shanghai）：`formatRainSummary` 现在
  保持降雨窗口按时间排序，并将每个窗口的时间与自身 `maxProbability` 一起输出；最高概率窗口
  从同一 `rainWindows` 集合中选择，不再把全局最高概率和首个窗口拼接。新增多窗口回归用例，验证
  `04:00—10:00` 的 40% 与 `16:00—20:00` 的 70% 不会错配；`personal-weather` 的 12 个测试
  文件、103 个测试全部通过，TypeScript build 与 `openclaw config validate` 也通过。另在
  `chatbot/AGENTS.md` 的通用回答质量规则中加入时区/UTC 识别提示，不绑定天气或某个固定地区；
  随后的真实 QQ 天气重测结果见下一条记录。
- 天气 formatter 修复后的真实 QQ 重测（2026-08-12 20:13，Asia/Shanghai）已通过：会话日志显示主
  Agent 实际调用 `personal_weather_get_brief`，未调用 `personal_web_search`；工具返回的 UTC 窗口
  `20:00Z—02:00Z`、`04:00Z—05:00Z`、`06:00Z—07:00Z`、`08:00Z—13:00Z` 被正确换算为本地
  `04:00—10:00`（40%）、`12:00—13:00`（42%）、`14:00—15:00`（34%）、`16:00—21:00`
  （77%）。最终回答逐项保留了窗口与概率的绑定，并将最高 77% 指向 `16:00—21:00`，同时明确
  `Asia/Shanghai` 与查询时间。该次天气运行时回归通过；它不改变规划/提醒真实投递回归仍待完成的
  状态。
- 规划/提醒只读联合真实 QQ 回归（2026-08-12 20:17，Asia/Shanghai）已通过：主人从新会话发起
  只读查询，主 Agent 并行调用 `personal_planning_state_get` 与 `personal_reminder_state_get`，
  没有调用 `personal_web_search`，也没有调用任何 proposal/commit/change/cancel 工具。回答正确
  汇总了默认天气地点与 `10:30` 简报、1 条既有 `planned` 无锡航班、1 条历史 pending 行程提案
  及其过期时间，以及“当前没有活动提醒”；工具和日志均未显示本轮产生写入或 Cron 变更。该次证明
  规划/提醒的只读路由、权限边界和搜索隔离正常，不等于真实提醒投递、异常确认或停机恢复回归已完成。
- 提醒 proposal 预览真实 QQ 回归（2026-08-12 20:19，Asia/Shanghai）已通过：主人明确要求生成
  “15 分钟后提醒我”的一次性提案，主 Agent 实际调用 `personal_reminder_propose`，将相对时间
  换算为 `2026-08-12 20:34`（`Asia/Shanghai`），返回 `pending`、内容和仅主人 QQ 私聊投递的
  预览，并明确尚未提交、尚未创建 Cron。该步骤按设计仅持久化一条 `reminder_proposals` 草稿，
  不创建正式提醒；日志未出现 `personal_reminder_commit`、其他修改/取消工具或
  `personal_web_search`。提案确认提交和实际投递结果见后续记录。
- 提醒 proposal 提交真实 QQ 回归（2026-08-12 20:21，Asia/Shanghai）已通过：主人在同一私聊中
  明确确认提案，主 Agent 使用预览返回的准确 proposal ID 与 payload Hash 调用
  `personal_reminder_commit`，工具返回 `scheduled`，时间为 `20:34`，无 warning。随后读取 Gateway
  Cron 列表，确认该提醒只注册了一条受管任务，目标为主人 QQ 私聊，未产生重复任务；回复正确使用
  “已登记为 scheduled/到点投递”，没有提前声称已送达。实际投递结果见下一条记录。
- 提醒实际投递真实 QQ 回归（2026-08-12 20:34，Asia/Shanghai）已通过：主人确认收到“主人，提醒
  时间到了：提醒提案回归测试”。`openclaw cron runs --id` 显示该受管任务于 `20:34:00` 执行，
  固定 `reminder-cli.js deliver` 返回 0，`status=ok`、`delivered=true`、`deliveryStatus=delivered`、
  运行次数为 1，目标为主人 QQ 私聊；Gateway 对账随后记录 `delivered=1`、`failed=0`、`retries=0`、
  `unknown=0`。投递后活动 Cron 列表只剩每日天气简报，证明一次性提醒已清理且没有重复任务。
- 提醒重复确认负向真实 QQ 回归（2026-08-12 20:45，Asia/Shanghai）已通过：主人在原私聊中再次
  发送“确认”，主 Agent 使用同一 proposal ID/Hash 再次调用 `personal_reminder_commit`；工具返回
  `idempotent=true`，既有提醒终态为 `delivered`，没有创建第二条 Cron，活动列表仍只剩每日天气简报。
  原始工具结果的外层 `status` 为 `scheduling_failed`，但 `warnings` 为空且嵌套提醒状态明确为
  `delivered`；主 Agent 正确以已完成的幂等结果向主人解释，没有把它误报为新的失败或重复投递。
  该次完成的是提醒侧幂等/重复确认回归，不等于受控搜索的权限或上游故障负向验收。

官方依据：

- DeepSeek [Responses API](https://api-docs.deepseek.com/zh-cn/guides/responses_api/)；
- DeepSeek [Tool Calls](https://api-docs.deepseek.com/guides/tool_calls/)；
- OpenClaw [DeepSeek Provider](https://docs.openclaw.ai/providers/deepseek)；
- OpenClaw [Web Search](https://docs.openclaw.ai/tools/web)。

S0 状态说明：

- S0.1、S0.3、S0.6、S0.7 已由本文的范围、配置契约、模块边界和验收/回滚矩阵满足；
- S0.2 尚未完成 live 验收；当前优先验证 DeepSeek Responses Web Search，Brave、模型原生搜索、
  其他托管 Provider 或自托管方案保留为后备选项。仍需确认凭证/登录、账户硬限额、数据外发和
  结果契约；
- S0.8 在 S0.2 完成前不勾选，但这不阻止无网络权限的 mock 实现与离线测试。
